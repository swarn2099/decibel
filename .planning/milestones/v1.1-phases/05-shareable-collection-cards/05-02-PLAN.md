---
phase: 05-shareable-collection-cards
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/app/fan/[id]/card/card-client.tsx
  - src/app/fan/[id]/card/page.tsx
autonomous: false
requirements: [SHARE-04]

must_haves:
  truths:
    - "Fan can copy the card URL to share it"
    - "Card URL shared on Twitter/iMessage shows correct 1200x630 preview"
  artifacts:
    - path: "src/app/fan/[id]/card/card-client.tsx"
      provides: "Client component with copy-to-clipboard share button"
      min_lines: 20
  key_links:
    - from: "src/app/fan/[id]/card/card-client.tsx"
      to: "navigator.clipboard"
      via: "Copy current URL to clipboard on button click"
      pattern: "navigator\\.clipboard\\.writeText"
---

<objective>
Add share UX to the fan collection card and verify social preview rendering.

Purpose: Let fans easily share their card URL and confirm OG previews render correctly on major platforms.
Output: Share/copy button on card page, verified social previews.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-shareable-collection-cards/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Share button client component + integration</name>
  <files>src/app/fan/[id]/card/card-client.tsx, src/app/fan/[id]/card/page.tsx</files>
  <action>
1. Create `src/app/fan/[id]/card/card-client.tsx` — a client component ("use client") that renders:
   - A "Share Collection" button styled with Decibel aesthetic (gradient bg from pink to purple, white text, rounded-lg, px-6 py-3)
   - On click: copies the current page URL (`window.location.href`) to clipboard via `navigator.clipboard.writeText()`
   - Shows a toast notification via sonner (`toast.success("Link copied!")`) on successful copy
   - Also renders a "Share on X" link that opens `https://twitter.com/intent/tweet?url={encodedURL}&text={encodedText}` in a new tab. Text: "Check out my collection on @decabordelive"
   - Import Share2 and Copy icons from lucide-react

2. Update `src/app/fan/[id]/card/page.tsx` to:
   - Import and render `CardClient` below the card content
   - Ensure the share buttons are positioned below the card, centered
   - Add `<Toaster />` from sonner if not already in layout (check layout.tsx first — if Toaster is already in root layout, skip this)

Build must pass: `npm run build`
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Share button renders on card page, copies URL to clipboard with toast feedback, "Share on X" link opens Twitter intent</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify card rendering and social preview</name>
  <files>none</files>
  <action>Human verifies the complete fan collection card flow works end-to-end including OG previews on social platforms.</action>
  <verify>Human confirms card renders correctly and OG preview works on at least two platforms</verify>
  <done>Card page approved with working share functionality and social previews</done>
  <what-built>Fan collection card page with OG image and share functionality at /fan/[id]/card</what-built>
  <how-to-verify>
    1. Find a fan ID from the database (check Supabase fans table)
    2. Visit the deployed card page: https://decibel-swarn-singhs-projects.vercel.app/fan/{fan-id}/card
    3. Verify the card shows: dark background, artist photos in grid, tier badges, total count, DECIBEL branding
    4. Click "Share Collection" button — should copy URL and show toast
    5. Test OG preview: paste the card URL into https://www.opengraph.xyz/ or Twitter card validator
    6. Verify the preview shows a 1200x630 image with artist photos and fan name
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. Share button copies URL and shows toast
3. OG image renders at correct dimensions when URL is shared
4. Social preview validated on at least one platform
</verification>

<success_criteria>
- Fan can copy their card URL with one click
- Shared URL renders correct OG preview on Twitter/iMessage
- Full flow works: visit card -> see collection -> share -> preview renders
</success_criteria>

<output>
After completion, create `.planning/phases/05-shareable-collection-cards/05-02-SUMMARY.md`
</output>
