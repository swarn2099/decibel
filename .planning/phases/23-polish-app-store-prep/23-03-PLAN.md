---
phase: 23-polish-app-store-prep
plan: 03
type: execute
wave: 2
depends_on: [23-01, 23-02]
files_modified:
  - /home/swarn/decibel-mobile/app.json
  - /home/swarn/decibel-mobile/eas.json
autonomous: false
requirements: [POLISH-09, POLISH-10]

must_haves:
  truths:
    - "TestFlight build can be downloaded and installed by beta testers via TestFlight invite link"
    - "App Store screenshots exist for 6.7-inch and 5.5-inch device sizes showing key screens"
  artifacts:
    - path: "app.json"
      provides: "Production-ready Expo config with bundleIdentifier, version, permissions"
      contains: "bundleIdentifier"
    - path: "eas.json"
      provides: "EAS build profiles with production config for TestFlight"
      contains: "production"
  key_links:
    - from: "eas.json"
      to: "app.json"
      via: "EAS reads expo config for build settings"
      pattern: "production"
---

<objective>
Prepare the app for TestFlight distribution and generate App Store screenshots for required device sizes.

Purpose: This is the final step before beta testing. The app needs a proper bundleIdentifier, production EAS config, and marketing screenshots showing the key flows (passport, map, collection, leaderboard).
Output: Submittable TestFlight build and App Store screenshot set.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/swarn/decibel-mobile/app.json
@/home/swarn/decibel-mobile/eas.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure production app.json and prepare EAS build for TestFlight</name>
  <files>
    /home/swarn/decibel-mobile/app.json,
    /home/swarn/decibel-mobile/eas.json
  </files>
  <action>
    **app.json updates:**
    - Set `expo.name` to "Decibel"
    - Set `expo.slug` to "decibel"
    - Set `expo.version` to "1.0.0"
    - Set `expo.ios.bundleIdentifier` to "com.decibel.app" (or ask Swarn if he has a preferred identifier)
    - Set `expo.android.package` to "com.decibel.app"
    - Set `expo.ios.buildNumber` to "1"
    - Set `expo.userInterfaceStyle` to "dark" (the underground doesn't have a light mode)
    - Set `expo.splash.backgroundColor` to "#0B0B0F" (match app bg, not white)
    - Remove the light-colored splash defaults (white bg, light adaptive icon bg)
    - Set `expo.android.adaptiveIcon.backgroundColor` to "#0B0B0F"
    - Add `expo.ios.infoPlist` with:
      - `NSLocationWhenInUseUsageDescription`: "Decibel uses your location to detect when you're at a live show so you can collect the performing artists."
      - `NSCameraUsageDescription`: "Decibel uses your camera to scan QR codes at live shows."
    - Ensure all plugins are listed: expo-router, expo-font, expo-secure-store, expo-linking, expo-location, expo-notifications, expo-haptics
    - Add `expo.extra.eas.projectId` if not present (will need the EAS project ID -- check with `eas project:info` or create one)

    **eas.json updates:**
    - Ensure `production` profile has `autoIncrement: true`
    - Add `submit.production.ios.appleId` and `submit.production.ios.ascAppId` placeholders with TODO comments for Swarn to fill
    - The preview profile should use `distribution: "internal"` (already set)

    **Build trigger:**
    - Run `cd /home/swarn/decibel-mobile && eas build --platform ios --profile preview --non-interactive` to create a TestFlight-ready build
    - If EAS CLI not authenticated, this will fail -- create a checkpoint for Swarn to authenticate
    - If build succeeds, note the build URL in the summary
    - Do NOT run the production profile yet -- preview is sufficient for TestFlight internal testing
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && cat app.json | python3 -c "import sys,json; d=json.load(sys.stdin)['expo']; assert d.get('ios',{}).get('bundleIdentifier'), 'missing bundleId'; assert d['userInterfaceStyle']=='dark', 'not dark mode'; assert d['splash']['backgroundColor']=='#0B0B0F', 'splash bg wrong'; print('app.json OK')"</automated>
  </verify>
  <done>app.json has production-ready config with bundleIdentifier, dark splash screen, proper permission descriptions, all plugins listed. EAS build triggered (or checkpoint created if auth needed).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify TestFlight config and build status</name>
  <files>/home/swarn/decibel-mobile/app.json</files>
  <action>Human verification of production config and EAS build. Swarn confirms bundleIdentifier, checks build status, and captures App Store screenshots manually from simulator at required sizes.</action>
  <verify>Swarn confirms "approved"</verify>
  <done>TestFlight config approved, build submitted or auth completed, screenshots captured for 6.7" and 5.5" sizes.</done>
  <what-built>Production-ready app.json and EAS configuration. TestFlight build initiated (or auth checkpoint reached). The app is configured with bundleIdentifier "com.decibel.app", dark splash screen, proper iOS permission strings, and all required Expo plugins.</what-built>
  <how-to-verify>
    1. Check app.json looks correct: `cat /home/swarn/decibel-mobile/app.json | python3 -m json.tool`
    2. If EAS build was triggered, check build status: `cd /home/swarn/decibel-mobile && eas build:list --limit 1`
    3. If EAS auth was needed, run: `eas login` then `eas build --platform ios --profile preview`
    4. Confirm bundleIdentifier "com.decibel.app" is what you want (or specify a different one)
    5. For App Store screenshots: capture from simulator showing passport, map, collection confirmation, and leaderboard at 6.7" (iPhone 15 Pro Max, 1290x2796) and 5.5" (iPhone 8 Plus, 1242x2208)
  </how-to-verify>
  <resume-signal>Type "approved" if config looks good, or specify changes needed</resume-signal>
</task>

</tasks>

<verification>
- app.json has bundleIdentifier set
- app.json has dark userInterfaceStyle and dark splash bg
- app.json has iOS permission descriptions for location and camera
- eas.json has production profile with autoIncrement
- EAS build status checked
</verification>

<success_criteria>
- app.json production-ready with bundleIdentifier, dark mode, permissions, all plugins
- EAS build triggered or auth checkpoint reached
- Swarn can download TestFlight build after approval
- App Store screenshot requirements documented (6.7" iPhone 15 Pro Max, 5.5" iPhone 8 Plus)
</success_criteria>

<output>
After completion, create `.planning/phases/23-polish-app-store-prep/23-03-SUMMARY.md`
</output>
