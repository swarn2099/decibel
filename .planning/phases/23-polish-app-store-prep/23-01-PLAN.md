---
phase: 23-polish-app-store-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/swarn/decibel-mobile/src/components/ui/SkeletonLoader.tsx
  - /home/swarn/decibel-mobile/src/components/ui/ErrorState.tsx
  - /home/swarn/decibel-mobile/src/components/ui/EmptyState.tsx
  - /home/swarn/decibel-mobile/app/(tabs)/index.tsx
  - /home/swarn/decibel-mobile/app/(tabs)/passport.tsx
  - /home/swarn/decibel-mobile/app/(tabs)/search.tsx
  - /home/swarn/decibel-mobile/app/(tabs)/collect.tsx
  - /home/swarn/decibel-mobile/app/(tabs)/map.tsx
  - /home/swarn/decibel-mobile/app/artist/[id].tsx
  - /home/swarn/decibel-mobile/app/leaderboard.tsx
autonomous: true
requirements: [POLISH-01, POLISH-07, POLISH-08]

must_haves:
  truths:
    - "All data-dependent screens show skeleton placeholders while loading (not just a spinner)"
    - "When network request fails, screen shows error message with a Retry button that re-fetches"
    - "Screens with empty data show branded illustration and helpful message instead of blank space"
  artifacts:
    - path: "src/components/ui/SkeletonLoader.tsx"
      provides: "Reusable skeleton shimmer component with configurable shapes (rect, circle, row)"
      min_lines: 40
    - path: "src/components/ui/ErrorState.tsx"
      provides: "Network error display with retry callback"
      min_lines: 25
    - path: "src/components/ui/EmptyState.tsx"
      provides: "Empty state with icon, title, subtitle, optional CTA"
      min_lines: 25
  key_links:
    - from: "app/(tabs)/index.tsx"
      to: "src/components/ui/SkeletonLoader.tsx"
      via: "import and render when isLoading"
      pattern: "SkeletonLoader"
    - from: "app/(tabs)/passport.tsx"
      to: "src/components/ui/EmptyState.tsx"
      via: "render when collections list is empty"
      pattern: "EmptyState"
    - from: "app/(tabs)/index.tsx"
      to: "src/components/ui/ErrorState.tsx"
      via: "render when isError from TanStack Query"
      pattern: "ErrorState"
---

<objective>
Add skeleton loading states, network error states with retry, and empty state illustrations across all data-dependent screens in the Decibel mobile app.

Purpose: Replace generic ActivityIndicator spinners with polished skeleton loaders, add proper error handling with retry buttons, and show branded empty states instead of blank screens -- making the app feel production-ready.
Output: Three reusable UI components (SkeletonLoader, ErrorState, EmptyState) integrated into all tab screens and detail screens.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/swarn/decibel-mobile/src/components/ui/Button.tsx
@/home/swarn/decibel-mobile/src/constants/colors.ts
@/home/swarn/decibel-mobile/app/(tabs)/index.tsx
@/home/swarn/decibel-mobile/app/(tabs)/passport.tsx

<interfaces>
<!-- Existing TanStack Query pattern used across all hooks -->
<!-- Every hook returns { data, isLoading, isError, error, refetch } -->
<!-- Screens currently use isLoading + ActivityIndicator pattern -->

From src/constants/colors.ts:
```typescript
export const Colors = {
  bg: '#0B0B0F',
  card: '#1A1A24',
  white: '#FFFFFF',
  gray: '#8B8B9E',
  lightGray: '#6B6B7E',
  pink: '#FF4D6A',
  purple: '#9B6DFF',
  blue: '#4D9AFF',
  teal: '#00D4AA',
  yellow: '#FFD700',
  // ...
};
```

From app/(tabs)/index.tsx (current loading pattern to replace):
```typescript
{isLoading && (
  <View className="items-center py-12">
    <ActivityIndicator color={Colors.pink} size="large" />
  </View>
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable SkeletonLoader, ErrorState, and EmptyState UI components</name>
  <files>
    /home/swarn/decibel-mobile/src/components/ui/SkeletonLoader.tsx,
    /home/swarn/decibel-mobile/src/components/ui/ErrorState.tsx,
    /home/swarn/decibel-mobile/src/components/ui/EmptyState.tsx
  </files>
  <action>
    **SkeletonLoader.tsx:**
    Create a shimmer skeleton component using Reanimated. Build composable skeleton shapes:
    - `SkeletonRect` (width, height, borderRadius) -- for cards, banners
    - `SkeletonCircle` (size) -- for avatars
    - `SkeletonLine` (width as % or number, height defaults to 14) -- for text lines
    - `SkeletonGroup` -- container with gap prop
    - Use a Reanimated loop (useSharedValue + withRepeat + withTiming) to animate opacity from 0.3 to 0.7 on all shapes, giving a pulse/shimmer effect
    - Background color: Colors.card (#1A1A24), use opacity animation (NOT a gradient sweep -- keep it simple and performant)
    - Export preset compositions: `HomeFeedSkeleton`, `ArtistProfileSkeleton`, `PassportSkeleton`, `LeaderboardSkeleton` that mirror the actual layout of each screen (use rough sizing)
    - HomeFeedSkeleton: title area, 2 event cards (rect w/ small circle + lines), horizontal artist row (5 circles with line below), another section header + row
    - ArtistProfileSkeleton: hero rect (full width, 250h), 2 stat circles row, section with 3 line items
    - PassportSkeleton: avatar circle + 2 text lines, stats bar (4 small rects), grid of 6 stamp squares
    - LeaderboardSkeleton: 3 podium rects, 8 rank rows (circle + line + line)

    **ErrorState.tsx:**
    Full-screen centered error component. Props: `message?: string`, `onRetry: () => void`.
    - Wifi-off icon from lucide-react-native (WifiOff)
    - Title: "Connection Error" (Poppins_600SemiBold, 18px, Colors.white)
    - Subtitle: message or "Something went wrong. Check your connection and try again." (Poppins_400Regular, 14px, Colors.gray)
    - Retry button: pink background (#FF4D6A), white text "Retry", rounded-2xl, onPress calls onRetry
    - Dark background matching app bg (#0B0B0F)

    **EmptyState.tsx:**
    Centered empty state. Props: `icon: React.ReactNode`, `title: string`, `subtitle: string`, `ctaLabel?: string`, `onCta?: () => void`.
    - Renders the passed icon (lucide icon) at 64px in Colors.lightGray
    - Title in Poppins_600SemiBold, 18px, Colors.white
    - Subtitle in Poppins_400Regular, 14px, Colors.gray, textAlign center, max-width 280
    - Optional CTA button (same style as ErrorState retry but purple)
    - Centered vertically with some top offset (paddingTop: 60) so it doesn't look awkwardly centered
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Three UI components exist and type-check. SkeletonLoader exports preset skeleton compositions for each major screen. ErrorState accepts onRetry callback. EmptyState accepts icon/title/subtitle/cta props.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate skeleton/error/empty states into all data-dependent screens</name>
  <files>
    /home/swarn/decibel-mobile/app/(tabs)/index.tsx,
    /home/swarn/decibel-mobile/app/(tabs)/passport.tsx,
    /home/swarn/decibel-mobile/app/(tabs)/search.tsx,
    /home/swarn/decibel-mobile/app/(tabs)/collect.tsx,
    /home/swarn/decibel-mobile/app/(tabs)/map.tsx,
    /home/swarn/decibel-mobile/app/artist/[id].tsx,
    /home/swarn/decibel-mobile/app/leaderboard.tsx
  </files>
  <action>
    For each screen, replace the current loading/empty patterns:

    **Home (index.tsx):**
    - Replace `ActivityIndicator` block with `<HomeFeedSkeleton />` when isLoading
    - Add `<ErrorState onRetry={() => { events.refetch(); residents.refetch(); recentlyAdded.refetch(); }} />` when any query isError
    - Empty events section: show EmptyState with Calendar icon, "No upcoming events", "Check back closer to the weekend"

    **Passport (passport.tsx):**
    - Replace loading spinner with `<PassportSkeleton />`
    - Add error state with retry for profile/collections query
    - When collections list empty: EmptyState with Disc icon, "No collections yet", "Start collecting artists at shows or discover them online", CTA "Explore Artists" -> router.push('/search')
    - When badges all locked: EmptyState with Award icon, "No badges earned yet", "Collect more artists to unlock badges"

    **Search (search.tsx):**
    - When search is idle (no query entered): show subtle prompt, not empty state (this is the default expected state)
    - When search returns no results: EmptyState with Search icon, "No artists found", "Try a different name or add them from Spotify"
    - Error state with retry on search query failure

    **Collect (collect.tsx):**
    - Skeleton loader for nearby venue detection
    - Error state for location query failures
    - EmptyState with MapPin icon when no venues nearby: "No shows nearby", "Try the manual 'I'm at a show' button above"

    **Map (map.tsx):**
    - Skeleton for map loading (just a full-screen dark rect with a few marker circles)
    - Error state with retry for venue data fetch failure

    **Artist Profile (artist/[id].tsx):**
    - Replace loading state with `<ArtistProfileSkeleton />`
    - Error state with retry + Back button option

    **Leaderboard (leaderboard.tsx):**
    - Replace loading with `<LeaderboardSkeleton />`
    - Error state with retry
    - Empty leaderboard: EmptyState with Trophy icon, "No rankings yet", "Be the first to collect artists and claim the top spot"

    Pattern for all screens: check `isError` BEFORE `isLoading` (error takes priority). Use the TanStack Query `refetch()` method for retry.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Every data-dependent screen shows skeleton loaders during loading, error states with retry on failure, and branded empty states when data is empty. No more raw ActivityIndicator spinners in any tab or detail screen.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `cd /home/swarn/decibel-mobile && npx tsc --noEmit`
- All 7 screen files import and use SkeletonLoader/ErrorState/EmptyState appropriately
- No remaining ActivityIndicator imports in tab screens (except RefreshControl which is fine): `grep -rn "ActivityIndicator" /home/swarn/decibel-mobile/app/ --include="*.tsx" 2>/dev/null`
</verification>

<success_criteria>
- SkeletonLoader, ErrorState, EmptyState components created and exported
- Home, Passport, Search, Collect, Map, Artist, Leaderboard screens all use skeleton loading
- All screens show error state with retry button when TanStack Query returns isError
- Empty data states show branded illustrations with helpful messages
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/23-polish-app-store-prep/23-01-SUMMARY.md`
</output>
