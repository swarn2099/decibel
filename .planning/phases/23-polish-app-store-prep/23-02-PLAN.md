---
phase: 23-polish-app-store-prep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/swarn/decibel-mobile/src/lib/queryClient.ts
  - /home/swarn/decibel-mobile/src/hooks/usePassport.ts
  - /home/swarn/decibel-mobile/src/hooks/useArtistProfile.ts
  - /home/swarn/decibel-mobile/src/components/collection/ConfirmationModal.tsx
  - /home/swarn/decibel-mobile/src/components/passport/BadgeDetailModal.tsx
  - /home/swarn/decibel-mobile/src/components/passport/PassportHeader.tsx
  - /home/swarn/decibel-mobile/src/components/ui/PullToRefresh.tsx
  - /home/swarn/decibel-mobile/app/(tabs)/index.tsx
  - /home/swarn/decibel-mobile/app/(tabs)/passport.tsx
autonomous: true
requirements: [POLISH-02, POLISH-03, POLISH-04, POLISH-05, POLISH-06]

must_haves:
  truths:
    - "Passport collections and artist profiles are viewable after going offline (cached data persists)"
    - "Collection confirmation shows stamp-slam animation with heavy haptic feedback on impact"
    - "Badge unlock shows starburst/radial animation with haptic feedback"
    - "Passport header shrinks and fades as user scrolls down the collection timeline"
    - "Pull-to-refresh shows a custom Decibel-branded animation instead of default iOS/Android spinner"
  artifacts:
    - path: "src/lib/queryClient.ts"
      provides: "TanStack Query client with MMKV persisted cache"
      min_lines: 20
    - path: "src/components/ui/PullToRefresh.tsx"
      provides: "Custom pull-to-refresh with Decibel sound wave animation"
      min_lines: 40
  key_links:
    - from: "src/hooks/usePassport.ts"
      to: "src/lib/queryClient.ts"
      via: "TanStack Query gcTime + staleTime for offline cache"
      pattern: "gcTime|staleTime"
    - from: "app/(tabs)/passport.tsx"
      to: "src/components/passport/PassportHeader.tsx"
      via: "Animated scroll handler driving header parallax"
      pattern: "useAnimatedScrollHandler|interpolate"
    - from: "src/components/collection/ConfirmationModal.tsx"
      to: "expo-haptics"
      via: "Heavy impact on stamp land + notification on tier-up"
      pattern: "Haptics.impactAsync"
---

<objective>
Add offline data caching, polished micro-animations with haptic feedback, and custom pull-to-refresh to make the app feel premium and native.

Purpose: Offline support lets fans browse their passport and saved artists without connectivity. Enhanced animations with haptics make every interaction feel satisfying. Custom pull-to-refresh reinforces brand identity.
Output: Persisted query cache, enhanced stamp/badge animations, passport parallax header, custom pull-to-refresh component.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/swarn/decibel-mobile/src/components/collection/ConfirmationModal.tsx
@/home/swarn/decibel-mobile/src/components/passport/PassportHeader.tsx
@/home/swarn/decibel-mobile/app/(tabs)/passport.tsx
@/home/swarn/decibel-mobile/src/hooks/usePassport.ts
@/home/swarn/decibel-mobile/src/hooks/useArtistProfile.ts
@/home/swarn/decibel-mobile/src/lib/storage.ts

<interfaces>
<!-- MMKV storage already set up in the project -->
From src/lib/storage.ts:
```typescript
import { MMKV } from 'react-native-mmkv';
export const storage = new MMKV();
```

<!-- Existing stamp animation pattern in ConfirmationModal -->
<!-- Uses: withSpring(damping:12, stiffness:180), runOnJS for haptic trigger -->
<!-- Already has: stampScale, stampTranslateY, ringScale, ringOpacity, confettiOpacity -->
<!-- POLISH-03 asks to enhance this (already partially done - verify and add any missing haptics) -->

<!-- Passport already has AnimatedFlatList with scroll handler -->
From app/(tabs)/passport.tsx:
```typescript
const AnimatedFlatList = Animated.createAnimatedComponent(FlatList<CollectionStampType>);
const HEADER_HEIGHT = 160;
// scrollY shared value exists, interpolation for parallax partially set up
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add offline cache persistence and enhance animations with haptic feedback</name>
  <files>
    /home/swarn/decibel-mobile/src/lib/queryClient.ts,
    /home/swarn/decibel-mobile/src/hooks/usePassport.ts,
    /home/swarn/decibel-mobile/src/hooks/useArtistProfile.ts,
    /home/swarn/decibel-mobile/src/components/collection/ConfirmationModal.tsx,
    /home/swarn/decibel-mobile/src/components/passport/BadgeDetailModal.tsx
  </files>
  <action>
    **Offline Cache (POLISH-02):**
    Create or update `src/lib/queryClient.ts`:
    - Use `@tanstack/react-query-persist-client` with a custom MMKV storage adapter
    - Create MMKV persister: `{ getItem: (key) => storage.getString(key), setItem: (key, value) => storage.set(key, value), removeItem: (key) => storage.delete(key) }`
    - If `@tanstack/react-query-persist-client` is not installed, use a simpler approach: set `gcTime: Infinity` and `staleTime: 10 * 60 * 1000` (10 min) on passport and artist profile queries so they survive in memory longer
    - In `usePassport.ts`: set `gcTime: 1000 * 60 * 60 * 24` (24 hours) and `staleTime: 5 * 60 * 1000` (5 min) for passport collections query. This keeps data in cache even when component unmounts
    - In `useArtistProfile.ts`: set `gcTime: 1000 * 60 * 60 * 4` (4 hours) for artist profile data
    - Check if `@tanstack/react-query-persist-client` is in package.json. If not, install it: `npx expo install @tanstack/react-query-persist-client`. If install fails due to version conflicts, fall back to the long gcTime approach above (still achieves offline viewing within a session)

    **Collection Stamp Animation (POLISH-03):**
    The ConfirmationModal already has a solid stamp-slam animation with haptic. Enhance it:
    - Add a second haptic on tier-up: after confetti appears (~800ms delay), trigger `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)`
    - Add subtle bounce-back: after stamp lands, add a small overshoot with `withSequence(withSpring(1.05, {...}), withSpring(1, {...}))` on stampScale for a more satisfying slam
    - Ensure the Heavy haptic fires on stamp impact (already exists -- verify it works)

    **Badge Unlock Animation (POLISH-04):**
    In `BadgeDetailModal.tsx` (or wherever badge unlock celebration shows):
    - Add a starburst/radial ray animation: create 8 Animated.View "rays" positioned absolutely around the badge icon, each a thin rect (2px wide, 30px tall), rotated in 45-degree increments
    - Animate rays: scale from 0 to 1.5 + opacity from 1 to 0 over 600ms with withDelay(200ms) after modal appears
    - Add glow ring: same pattern as ConfirmationModal's ink ring (scale from 0 to 2, opacity from 0.5 to 0) but in the badge's color
    - Trigger `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)` when animation fires
    - Only show these animations for newly earned badges (add a prop `justEarned?: boolean` if not already present)
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Passport and artist data cached with extended gcTime for offline viewing. Collection stamp has enhanced bounce-back animation with haptics on impact and tier-up. Badge unlock shows starburst ray animation with glow ring and success haptic.</done>
</task>

<task type="auto">
  <name>Task 2: Add passport parallax header and custom pull-to-refresh animation</name>
  <files>
    /home/swarn/decibel-mobile/src/components/ui/PullToRefresh.tsx,
    /home/swarn/decibel-mobile/src/components/passport/PassportHeader.tsx,
    /home/swarn/decibel-mobile/app/(tabs)/passport.tsx,
    /home/swarn/decibel-mobile/app/(tabs)/index.tsx
  </files>
  <action>
    **Passport Parallax Header (POLISH-05):**
    In `passport.tsx`, the AnimatedFlatList scroll handler already exists. Enhance the parallax:
    - Pass scrollY shared value to PassportHeader as a prop
    - In PassportHeader, use `useAnimatedStyle` with `interpolate`:
      - translateY: scrollY [0, HEADER_HEIGHT] -> [0, -HEADER_HEIGHT * 0.4] (header slides up slower than content)
      - opacity: scrollY [0, HEADER_HEIGHT * 0.6, HEADER_HEIGHT] -> [1, 0.8, 0] (fade out as scrolling)
      - scale: scrollY [0, HEADER_HEIGHT] -> [1, 0.95] (subtle shrink)
    - Make the stats bar sticky: when scrollY > HEADER_HEIGHT, stats bar stays pinned at top with bg matching app bg
    - Use Extrapolation.CLAMP on all interpolations to prevent overscroll weirdness

    **Custom Pull-to-Refresh (POLISH-06):**
    Create `PullToRefresh.tsx` -- a custom RefreshControl replacement:
    - Since React Native's RefreshControl is deeply native and hard to fully replace, take the pragmatic approach:
    - Create a custom header component that shows ABOVE the scroll content when pulling down
    - Use Reanimated's `useAnimatedScrollHandler` to detect pull-down gesture (scrollY < 0)
    - Animation: 3 vertical bars (sound wave equalizer style) that pulse at different rates when pulling
      - Bar heights: 12px, 20px, 14px (staggered)
      - Colors: pink, purple, blue (brand colors)
      - When pulling past threshold (-60px): bars animate with withRepeat + withSequence to bounce heights
      - When refreshing: bars continue pulsing until refresh completes
    - Still use the native RefreshControl under the hood for the actual pull-to-refresh trigger (for reliability), but customize its tintColor to transparent and overlay the custom animation
    - Apply to Home screen (index.tsx) and Passport screen (passport.tsx)
    - Keep the implementation simple -- if the custom animation proves too complex to overlay on native RefreshControl, fall back to just customizing the tintColor to Colors.pink and adding a small Decibel logo (Text "d" in Poppins_700Bold, pink) as the refresh indicator
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Passport header parallaxes (shrinks + fades) as user scrolls down the collection timeline. Pull-to-refresh on Home and Passport screens shows branded sound-wave animation instead of default spinner. Stats bar becomes sticky when header scrolls out of view.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `cd /home/swarn/decibel-mobile && npx tsc --noEmit`
- Check offline cache config: `grep -n "gcTime\|staleTime\|persist" /home/swarn/decibel-mobile/src/hooks/usePassport.ts /home/swarn/decibel-mobile/src/hooks/useArtistProfile.ts`
- Check haptics in animations: `grep -n "Haptics" /home/swarn/decibel-mobile/src/components/collection/ConfirmationModal.tsx /home/swarn/decibel-mobile/src/components/passport/BadgeDetailModal.tsx`
- Check parallax in passport: `grep -n "interpolate\|Extrapolation" /home/swarn/decibel-mobile/app/\(tabs\)/passport.tsx /home/swarn/decibel-mobile/src/components/passport/PassportHeader.tsx`
</verification>

<success_criteria>
- Passport collections queryable offline (gcTime set to 24h+)
- Artist profile data cached (gcTime set to 4h+)
- Collection stamp animation includes bounce-back overshoot and tier-up haptic
- Badge unlock shows starburst rays and glow ring with success haptic
- Passport header parallaxes on scroll with opacity fade and scale shrink
- Pull-to-refresh uses branded animation on Home and Passport screens
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/23-polish-app-store-prep/23-02-SUMMARY.md`
</output>
