---
phase: 10-scraper-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/scrapers/clean-performers.ts
  - scripts/scrapers/utils.ts
  - scripts/scrapers/ra.ts
  - scripts/scrapers/dice.ts
autonomous: true
requirements: [SCRP-01, SCRP-02]

must_haves:
  truths:
    - "No performers table rows contain event names instead of actual artist names"
    - "All Instagram handles in the database are stored as plain usernames (e.g., 'djname') not full URLs"
    - "Future scraper runs also normalize Instagram handles before insert"
  artifacts:
    - path: "scripts/scrapers/clean-performers.ts"
      provides: "One-shot DB cleanup script for bad performer names and Instagram URLs"
      min_lines: 80
    - path: "scripts/scrapers/utils.ts"
      provides: "Updated isNonArtistName with stronger heuristics"
      exports: ["isNonArtistName", "normalizeInstagramHandle"]
  key_links:
    - from: "scripts/scrapers/clean-performers.ts"
      to: "supabase performers table"
      via: "service role client"
      pattern: "supabase\\.from\\(\"performers\"\\)"
    - from: "scripts/scrapers/ra.ts"
      to: "scripts/scrapers/utils.ts"
      via: "normalizeInstagramHandle import"
      pattern: "normalizeInstagramHandle"
---

<objective>
Clean bad data from the performers table and harden scrapers against future bad inserts.

Purpose: The performers table has event names stored as artist names (e.g., "Shrek Rave", "90s Party Night") and Instagram handles stored as full URLs instead of plain usernames. This plan fixes existing data and prevents future occurrences.

Output: A cleanup script that identifies/removes bad performer entries and normalizes Instagram handles, plus hardened scraper utilities.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/scrapers/utils.ts
@scripts/scrapers/ra.ts
@scripts/scrapers/dice.ts
@scripts/scrapers/scrape-events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden utils and create cleanup script</name>
  <files>scripts/scrapers/utils.ts, scripts/scrapers/clean-performers.ts</files>
  <action>
1. In `scripts/scrapers/utils.ts`, strengthen `isNonArtistName()`:
   - Add patterns for: "rave" as standalone word at end (but NOT as part of a DJ name), "night" at end when preceded by adjective, "takeover", "residency", "open to close", "all night long", "b2b" as entire name, names that are ALL CAPS and >3 words (event titles tend to be ALL CAPS), "free entry", "no cover", "doors at", "21+", "18+", ticket price patterns like "$XX"
   - Add a word-count heuristic: names with 8+ words are almost certainly event titles
   - Reduce the existing 15-word threshold to 8 words

2. In `scripts/scrapers/utils.ts`, add a new exported function `normalizeInstagramHandle(raw: string): string`:
   - If input contains "instagram.com/", extract the path segment after it (strip query params, trailing slash)
   - Strip leading "@" if present
   - Return lowercase trimmed username
   - Return empty string if input is empty/null

3. Create `scripts/scrapers/clean-performers.ts`:
   - Fetch ALL performers from DB (`id, name, slug, instagram_handle, events(count)`)
   - Phase A — Bad names: Run each performer name through the updated `isNonArtistName()`. Log matches. For performers flagged as non-artist AND with 0-1 events, delete them (cascade: delete from events, collections, fan_tiers, messages, scraped_profiles first, then performers). For those with 2+ events, log as "review needed" but don't auto-delete.
   - Phase B — Instagram normalization: For every performer where `instagram_handle` contains "instagram.com" or starts with "http" or "www", run through `normalizeInstagramHandle()` and update the row.
   - Print summary: X performers deleted, Y Instagram handles normalized, Z flagged for review.
   - Use `--dry-run` flag (check process.argv) that logs what WOULD happen without making changes. Default to dry-run for safety.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && npx tsx scripts/scrapers/clean-performers.ts --dry-run 2>&1 | head -50</automated>
  </verify>
  <done>Cleanup script runs in dry-run mode showing performers to delete and Instagram handles to normalize. No data modified until --execute flag is passed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix scrapers to normalize Instagram on insert</name>
  <files>scripts/scrapers/ra.ts, scripts/scrapers/dice.ts</files>
  <action>
1. In `scripts/scrapers/ra.ts`:
   - Import `normalizeInstagramHandle` from `./utils`
   - Line ~292 where it does `artist.instagram.replace(/^@/, "")` — replace with `normalizeInstagramHandle(artist.instagram)`
   - Line ~325 where it does `artist.instagram?.replace(/^@/, "") || null` — replace with `artist.instagram ? normalizeInstagramHandle(artist.instagram) : null`
   - This ensures RA's GraphQL instagram field (which can return full URLs or @handles) is always stored as a plain username

2. In `scripts/scrapers/dice.ts`:
   - DICE doesn't currently capture Instagram, but if it starts returning social links in the future, the pattern is set via utils. No changes needed unless artist data includes instagram — verify and skip if not present.

3. In `scripts/scrapers/scrape-events.ts`:
   - EDMTrain doesn't provide Instagram data (sets `instagram_handle: null`), so no changes needed.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && grep -n "normalizeInstagramHandle" scripts/scrapers/ra.ts scripts/scrapers/utils.ts | head -20</automated>
  </verify>
  <done>RA scraper uses normalizeInstagramHandle for all Instagram handle storage. Future scraper runs will never insert full URLs as Instagram handles.</done>
</task>

</tasks>

<verification>
1. `npx tsx scripts/scrapers/clean-performers.ts --dry-run` runs without errors and identifies bad entries
2. `grep -n "normalizeInstagramHandle" scripts/scrapers/ra.ts` shows the function is imported and used
3. `grep -n "isNonArtistName" scripts/scrapers/utils.ts` shows strengthened patterns
</verification>

<success_criteria>
- Cleanup script identifies event-name-as-artist entries and Instagram URL entries in dry-run
- Running with --execute cleans the data (SCRP-01 and SCRP-02 satisfied)
- RA scraper hardened against future Instagram URL inserts
- isNonArtistName catches more edge cases for future scraper runs
</success_criteria>

<output>
After completion, create `.planning/phases/10-scraper-pipeline/10-01-SUMMARY.md`
</output>
