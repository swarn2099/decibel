---
phase: 18-passport-badges-sharing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - decibel-mobile/src/types/badges.ts
  - decibel-mobile/src/constants/badges.ts
  - decibel-mobile/src/hooks/useBadges.ts
  - decibel-mobile/src/components/passport/BadgeGrid.tsx
  - decibel-mobile/src/components/passport/BadgeDetailModal.tsx
autonomous: true
requirements:
  - PASS-04
  - PASS-05

must_haves:
  truths:
    - "Fan can view earned badges in a 3-column grid with icons and names"
    - "Earned badges are visually prominent (gold/metallic look), locked badges are dark silhouettes"
    - "Fan can tap an earned badge to see description, rarity, and date earned"
    - "Fan can tap a locked badge to see unlock requirements"
  artifacts:
    - path: "decibel-mobile/src/types/badges.ts"
      provides: "Badge type definitions mirroring web"
      exports: ["BadgeId", "BadgeDefinition", "EarnedBadge", "BadgeWithStatus"]
    - path: "decibel-mobile/src/constants/badges.ts"
      provides: "Badge definitions copied from web"
      exports: ["BADGE_DEFINITIONS"]
    - path: "decibel-mobile/src/hooks/useBadges.ts"
      provides: "Hook to fetch earned badges and merge with definitions"
      exports: ["useFanBadges"]
    - path: "decibel-mobile/src/components/passport/BadgeGrid.tsx"
      provides: "3-column grid of earned and locked badges"
      min_lines: 40
  key_links:
    - from: "decibel-mobile/src/hooks/useBadges.ts"
      to: "supabase"
      via: "Direct query to fan_badges table"
      pattern: "supabase.*from.*fan_badges"
    - from: "decibel-mobile/src/components/passport/BadgeGrid.tsx"
      to: "decibel-mobile/src/hooks/useBadges.ts"
      via: "useFanBadges hook"
      pattern: "useFanBadges"
---

<objective>
Build the badge system for the passport — a 3-column grid showing earned badges as metallic seals and locked badges as dark impressions, with detail modals on tap.

Purpose: Badges gamify the fan experience, making fans want to fill their passport grid. The visual contrast between earned and locked should drive engagement.
Output: Badge types, definitions, data hook, grid component, and detail modal — ready to be integrated into the passport screen by Plan 03.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-passport-badges-sharing/18-CONTEXT.md

<interfaces>
<!-- Web app badge types and definitions to port to mobile -->
From decibel/src/lib/types/badges.ts:
```typescript
export type BadgeCategory = "discovery" | "attendance" | "exploration" | "streak" | "social";
export type BadgeRarity = "common" | "rare" | "epic" | "legendary";
export type BadgeId = "trailblazer" | "first-100" | "first-10-verified" | "regular" | "devotee"
  | "inner-circle-badge" | "venue-local" | "venue-legend" | "genre-explorer" | "city-hopper"
  | "night-owl" | "scene-veteran" | "centurion" | "on-fire" | "unstoppable" | "year-round"
  | "tastemaker" | "connector";

export interface BadgeDefinition {
  id: BadgeId; name: string; description: string;
  category: BadgeCategory; rarity: BadgeRarity; icon: string; criteria: string;
}

export interface EarnedBadge { badge_id: BadgeId; fan_id: string; earned_at: string; }
```

From decibel/src/lib/badges/definitions.ts:
18 badge definitions with ids, names, descriptions, categories, rarities, icons (emoji), criteria strings. Port these exactly.

From decibel-mobile/src/stores/authStore.ts:
```typescript
export const useAuthStore = create<AuthState>(...);
// user.email available for fan lookup
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badge types, definitions, and data hook</name>
  <files>decibel-mobile/src/types/badges.ts, decibel-mobile/src/constants/badges.ts, decibel-mobile/src/hooks/useBadges.ts</files>
  <action>
Create `src/types/badges.ts` — port types from web:
- BadgeCategory, BadgeRarity, BadgeId (same union types)
- BadgeDefinition (id, name, description, category, rarity, icon, criteria)
- EarnedBadge (badge_id, fan_id, earned_at)
- BadgeWithStatus = BadgeDefinition & { earned: boolean; earned_at: string | null; rarity_percent: number | null }

Create `src/constants/badges.ts` — copy BADGE_DEFINITIONS from web's `src/lib/badges/definitions.ts` exactly (all 18 badges). Also export:
- `RARITY_COLORS`: Record mapping rarity to color (common=#8E8E9A, rare=#4D9AFF, epic=#9B6DFF, legendary=#FFD700)
- `getBadgesByCategory(category)` helper

Create `src/hooks/useBadges.ts`:
- `useFanBadges()` — TanStack Query hook:
  1. Get auth user email from useAuthStore
  2. Query fans table for fan_id by email
  3. Query fan_badges table for all earned badges (badge_id, earned_at) for this fan_id
  4. Merge with BADGE_DEFINITIONS: for each definition, mark as earned or locked
  5. For rarity_percent: query total fan count, then count fans who have each badge — compute percentage. Cache aggressively (staleTime: 30min) since rarity changes slowly.
  6. Return array of BadgeWithStatus sorted: earned first (by earned_at desc), then locked (alphabetically)
  7. staleTime: 10min for the main badge query
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Badge types match web app. All 18 definitions ported. Hook fetches earned badges from Supabase and merges with definitions, marking each as earned or locked with rarity percentage.</done>
</task>

<task type="auto">
  <name>Task 2: Badge grid and detail modal components</name>
  <files>decibel-mobile/src/components/passport/BadgeGrid.tsx, decibel-mobile/src/components/passport/BadgeDetailModal.tsx</files>
  <action>
Create `src/components/passport/BadgeGrid.tsx`:
- 3-column grid using FlatList with numColumns={3}
- Section header "Badges" with earned count (e.g., "Badges (4/18)")
- Each cell: circular badge icon (56px), badge name below in small text
- **Earned badges:** Background uses rarity color at 20% opacity, border in rarity color, icon emoji rendered large (28px), name in white. Give a subtle "embossed" look: inner shadow effect via nested Views with slightly darker background ring. Use gold (#FFD700) outer glow for legendary, purple for epic, blue for rare, gray for common.
- **Locked badges:** Background dark (#15151C), border dashed in lightGray, icon emoji at 30% opacity, name in lightGray. The "empty seal waiting to be pressed" look — use a circular View with dark inset appearance.
- On tap: open BadgeDetailModal with the badge data

Create `src/components/passport/BadgeDetailModal.tsx`:
- React Native Modal, dark backdrop (rgba(0,0,0,0.8))
- Centered card with bg-card (#15151C), rounded-2xl, padding 24px
- Badge icon large (48px) at top center
- Badge name in Poppins Bold, rarity label colored by rarity
- If earned: "Earned [date]" in teal, description, rarity percentage ("Top 12% of fans")
- If locked: "Locked" label in gray, criteria text explaining what's needed to unlock (from definition.criteria), description
- Close button (X) top-right
- DO NOT use white backgrounds in the modal

Both components receive badge data as props — they are presentation components. The passport screen (Plan 01) will integrate these by adding a "Badges" section below the collection timeline, calling useFanBadges() and rendering BadgeGrid.

NOTE: Plan 01 and Plan 02 run in parallel (same wave). Plan 03 will wire the BadgeGrid into the passport screen. For now, these components are self-contained and independently testable.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>BadgeGrid renders 3-column grid with visual distinction between earned (colored, prominent) and locked (dark, muted) badges. BadgeDetailModal shows full badge info on tap — earned badges show date and rarity, locked badges show unlock criteria. No white backgrounds.</done>
</task>

</tasks>

<verification>
- `cd /home/swarn/decibel-mobile && npx tsc --noEmit` — no type errors
- Badge definitions count: 18 badges across 5 categories
- BadgeGrid renders with numColumns={3}
- Visual distinction: earned badges are colored, locked are dark/muted
- Modal shows different content for earned vs locked badges
</verification>

<success_criteria>
- Fan can view badges in a 3-column grid with earned badges visually prominent and locked badges muted
- Tapping earned badge shows description, rarity percentage, earned date
- Tapping locked badge shows unlock requirements
- All 18 badge definitions ported from web app
- Hook fetches earned badges from Supabase fan_badges table
</success_criteria>

<output>
After completion, create `.planning/phases/18-passport-badges-sharing/18-02-SUMMARY.md`
</output>
