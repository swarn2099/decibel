---
phase: 20-location-based-collection
plan: 03
type: execute
wave: 1
depends_on: ["20-01", "20-02"]
files_modified:
  - /home/swarn/decibel/src/app/api/collect/route.ts
  - /home/swarn/decibel-mobile/src/hooks/useCollection.ts
  - /home/swarn/decibel-mobile/src/components/location/LocationBanner.tsx
  - /home/swarn/decibel-mobile/src/components/location/NearbyVenueSheet.tsx
  - /home/swarn/decibel-mobile/app/(tabs)/collect.tsx
autonomous: true
gap_closure: true
requirements: [LOC-01, LOC-02, LOC-03, LOC-04, LOC-05, LOC-06, LOC-07]

must_haves:
  truths:
    - "Location-based collections are stored with capture_method='location' in the database"
    - "QR-based and other existing collections still default to capture_method='qr'"
    - "Web API accepts optional capture_method from request body"
  artifacts:
    - path: "/home/swarn/decibel/src/app/api/collect/route.ts"
      provides: "Web API that reads capture_method from request body"
      contains: "capture_method"
    - path: "/home/swarn/decibel-mobile/src/hooks/useCollection.ts"
      provides: "useCollect hook accepting optional capture_method parameter"
      contains: "capture_method"
    - path: "/home/swarn/decibel-mobile/src/components/location/LocationBanner.tsx"
      provides: "Banner passing capture_method='location' to useCollect"
      contains: "location"
  key_links:
    - from: "/home/swarn/decibel-mobile/src/components/location/LocationBanner.tsx"
      to: "/home/swarn/decibel-mobile/src/hooks/useCollection.ts"
      via: "collect.mutate({ performerId, capture_method: 'location' })"
      pattern: "capture_method.*location"
    - from: "/home/swarn/decibel-mobile/src/hooks/useCollection.ts"
      to: "/home/swarn/decibel/src/app/api/collect/route.ts"
      via: "JSON.stringify includes capture_method"
      pattern: "capture_method"
    - from: "/home/swarn/decibel/src/app/api/collect/route.ts"
      to: "Supabase collections table"
      via: "insert with capture_method from body"
      pattern: "capture_method.*body"
---

<objective>
Fix capture_method passthrough so location-based collections are stored as 'location' instead of 'qr' in the database.

Purpose: The location detection, banner, and collection UI all work correctly, but every collection is recorded as capture_method='qr' because the web API hardcodes it and the mobile client never sends it. This is a data integrity issue — location-verified collections must be distinguishable from QR scans to support the "verified physical attendance" differentiator.

Output: Three-layer fix: web API reads capture_method from body, useCollect hook accepts and forwards it, all location-triggered collect calls pass 'location'.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-location-based-collection/20-VERIFICATION.md

<interfaces>
<!-- Current useCollect mutation type — needs capture_method added -->
From /home/swarn/decibel-mobile/src/hooks/useCollection.ts:
```typescript
export function useCollect() {
  return useMutation<CollectResult, Error, { performerId: string }>({
    mutationFn: async ({ performerId }) => {
      // ...
      body: JSON.stringify({ performer_id: performerId, email: user.email }),
      // ...
    },
  });
}
```

From /home/swarn/decibel/src/app/api/collect/route.ts:
```typescript
export async function POST(req: NextRequest) {
  const { performer_id, email } = await req.json();
  // Line 40: hardcoded capture_method: "qr"
  .insert({
    fan_id: fan.id,
    performer_id,
    event_date: today,
    capture_method: "qr",  // <-- THE BUG
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Web API + useCollect hook — accept and forward capture_method</name>
  <files>
    /home/swarn/decibel/src/app/api/collect/route.ts
    /home/swarn/decibel-mobile/src/hooks/useCollection.ts
  </files>
  <action>
1. In `/home/swarn/decibel/src/app/api/collect/route.ts`:
   - Line 13: Destructure `capture_method` from the request body alongside `performer_id` and `email`
   - Line 40: Replace the hardcoded `capture_method: "qr"` with `capture_method: capture_method || "qr"` — this defaults to 'qr' for backward compatibility (web QR scans don't send it)
   - Do NOT change any other logic. The validation on line 15 should NOT require capture_method (it's optional).

2. In `/home/swarn/decibel-mobile/src/hooks/useCollection.ts`:
   - Line 42: Change the mutation variable type from `{ performerId: string }` to `{ performerId: string; capture_method?: string }`
   - Line 43: Destructure `capture_method` alongside `performerId` in the mutationFn
   - Line 58: Add `capture_method` to the JSON.stringify body: `{ performer_id: performerId, email: user.email, capture_method }`
   - When capture_method is undefined (normal collect from artist profile), it won't be sent, and the API defaults to 'qr'. When 'location' is passed, it flows through.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && grep -n "capture_method" src/app/api/collect/route.ts | head -5 && cd /home/swarn/decibel-mobile && grep -n "capture_method" src/hooks/useCollection.ts | head -5</automated>
  </verify>
  <done>Web API reads capture_method from body with 'qr' fallback; useCollect accepts optional capture_method and includes it in the POST body</done>
</task>

<task type="auto">
  <name>Task 2: Location UI components pass capture_method='location'</name>
  <files>
    /home/swarn/decibel-mobile/src/components/location/LocationBanner.tsx
    /home/swarn/decibel-mobile/src/components/location/NearbyVenueSheet.tsx
    /home/swarn/decibel-mobile/app/(tabs)/collect.tsx
  </files>
  <action>
1. In `/home/swarn/decibel-mobile/src/components/location/LocationBanner.tsx`:
   - Line 138: Change `collect.mutate({ performerId: performer.id }, ...)` to `collect.mutate({ performerId: performer.id, capture_method: 'location' }, ...)`
   - This applies to the `handleCollect` function which is used by both single-artist and multi-artist layouts

2. In `/home/swarn/decibel-mobile/src/components/location/NearbyVenueSheet.tsx`:
   - Find the `collect.mutate({ performerId: performer.id }, ...)` call (around line 77)
   - Change to `collect.mutate({ performerId: performer.id, capture_method: 'location' }, ...)`
   - The NearbyVenueSheet is the "I'm at a show" manual trigger — it's still location-based collection since the fan confirmed they're at the venue

3. In `/home/swarn/decibel-mobile/app/(tabs)/collect.tsx`:
   - Find the `collect.mutate({ performerId: performer.id }, ...)` call (around line 72)
   - Change to `collect.mutate({ performerId: performer.id, capture_method: 'location' }, ...)`
   - The Collect tab's nearby event cards are also location-triggered collections

Do NOT change any other behavior — no UI changes, no new imports, no animation changes. This is a one-field addition to each mutate call.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && grep -rn "capture_method.*location" src/components/location/LocationBanner.tsx src/components/location/NearbyVenueSheet.tsx app/\(tabs\)/collect.tsx</automated>
  </verify>
  <done>All three location-triggered collect UI components pass capture_method='location' to useCollect; artist profile collect (not modified) continues to use default 'qr'</done>
</task>

</tasks>

<verification>
1. `grep -rn "capture_method" /home/swarn/decibel/src/app/api/collect/route.ts` — should show capture_method read from body and used in insert (no more hardcoded 'qr')
2. `grep -rn "capture_method" /home/swarn/decibel-mobile/src/hooks/useCollection.ts` — should show optional param in type and in JSON body
3. `grep -rn "capture_method.*location" /home/swarn/decibel-mobile/src/components/location/ /home/swarn/decibel-mobile/app/\(tabs\)/collect.tsx` — should show 3 files passing 'location'
4. `cd /home/swarn/decibel && npm run build` — web API build passes
5. `cd /home/swarn/decibel-mobile && npx tsc --noEmit` — mobile TypeScript compiles
</verification>

<success_criteria>
- Web API /api/collect accepts optional capture_method from request body, defaults to 'qr'
- Mobile useCollect hook forwards capture_method when provided
- LocationBanner, NearbyVenueSheet, and Collect tab all pass capture_method='location'
- Artist profile collect (no capture_method passed) still defaults to 'qr'
- Both web and mobile projects compile without errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-location-based-collection/20-03-SUMMARY.md`
</output>
