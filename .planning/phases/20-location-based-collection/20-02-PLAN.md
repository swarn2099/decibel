---
phase: 20-location-based-collection
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - /home/swarn/decibel-mobile/src/components/location/LocationBanner.tsx
  - /home/swarn/decibel-mobile/src/components/location/LocationPermissionModal.tsx
  - /home/swarn/decibel-mobile/src/components/location/NearbyVenueSheet.tsx
  - /home/swarn/decibel-mobile/app/(tabs)/collect.tsx
  - /home/swarn/decibel-mobile/app/(tabs)/index.tsx
  - /home/swarn/decibel-mobile/app/_layout.tsx
autonomous: true
requirements: [LOC-04, LOC-05, LOC-06]

must_haves:
  truths:
    - "When at a venue with an active event, a non-intrusive banner appears showing artist and venue"
    - "Multi-artist lineups show all performing artists with individual collect buttons"
    - "Fan can manually trigger 'I'm at a show' from the Collect tab as a fallback"
    - "Tapping X on the banner dismisses it and does not show again for that event"
    - "Tapping Collect on the banner creates a verified collection with capture_method='location'"
  artifacts:
    - path: "src/components/location/LocationBanner.tsx"
      provides: "Global overlay banner for venue detection prompts"
      min_lines: 40
    - path: "src/components/location/LocationPermissionModal.tsx"
      provides: "Explanation modal shown before OS permission prompt"
      min_lines: 30
    - path: "src/components/location/NearbyVenueSheet.tsx"
      provides: "Bottom sheet listing nearby venues for manual trigger"
      min_lines: 50
    - path: "app/(tabs)/collect.tsx"
      provides: "Collect tab with I'm at a show button and manual venue selection"
      min_lines: 40
  key_links:
    - from: "src/components/location/LocationBanner.tsx"
      to: "src/hooks/useVenueDetection.ts"
      via: "consumes ActiveVenueEvent[] to render banner"
      pattern: "useVenueDetection"
    - from: "src/components/location/LocationBanner.tsx"
      to: "src/hooks/useCollection.ts"
      via: "calls useCollect mutation on banner tap"
      pattern: "useCollect"
    - from: "app/_layout.tsx"
      to: "src/components/location/LocationBanner.tsx"
      via: "renders banner as global overlay above all screens"
      pattern: "LocationBanner"
    - from: "app/(tabs)/collect.tsx"
      to: "src/hooks/useVenueDetection.ts"
      via: "manual trigger uses same detection hook"
      pattern: "useVenueDetection"
---

<objective>
Build the location-based collection UI: the auto-detection banner overlay, permission explanation modal, "I'm at a show" manual trigger on the Collect tab, and multi-artist lineup display.

Purpose: This is the core mobile-native differentiator — fans get prompted to collect artists when physically present at a venue.
Output: Global LocationBanner overlay, LocationPermissionModal, Collect tab with manual trigger, NearbyVenueSheet for venue selection.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-location-based-collection/20-CONTEXT.md
@.planning/phases/20-location-based-collection/20-01-SUMMARY.md

Source files to reference:
@/home/swarn/decibel-mobile/app/_layout.tsx
@/home/swarn/decibel-mobile/app/(tabs)/collect.tsx
@/home/swarn/decibel-mobile/app/(tabs)/index.tsx
@/home/swarn/decibel-mobile/src/hooks/useCollection.ts
@/home/swarn/decibel-mobile/src/components/collection/CollectButton.tsx
@/home/swarn/decibel-mobile/src/components/collection/ConfirmationModal.tsx
@/home/swarn/decibel-mobile/src/constants/colors.ts

<interfaces>
<!-- From Plan 20-01 (must exist before this plan runs) -->

From src/hooks/useLocation.ts:
```typescript
export function useLocation(): {
  permissionStatus: 'granted' | 'denied' | 'undetermined';
  hasPermission: boolean;
  requestPermission: () => Promise<void>;
  getCurrentPosition: () => Promise<{latitude: number; longitude: number} | null>;
  explanationText: string;
};
```

From src/hooks/useVenueDetection.ts:
```typescript
export type ActiveVenueEvent = {
  venue: Venue;
  performers: Pick<Performer, 'id' | 'name' | 'slug' | 'photo_url'>[];
  eventId: string;
  eventDate: string;
  distance: number;
};

export function useVenueDetection(opts?: { enabled?: boolean }): {
  nearbyEvents: ActiveVenueEvent[];
  isChecking: boolean;
  refetch: () => void;
};
```

From src/stores/locationStore.ts:
```typescript
export const useLocationStore: {
  dismissedEventIds: string[];
  dismissEvent: (eventId: string) => void;
  isEventDismissed: (eventId: string) => boolean;
  hasShownExplanation: boolean;
  setExplanationShown: () => void;
  permissionDenied: boolean;
  setPermissionDenied: (denied: boolean) => void;
};
```

From src/hooks/useCollection.ts:
```typescript
export function useCollect(): UseMutationResult<CollectResult, Error, { performerId: string }>;
```

From src/constants/colors.ts:
```typescript
export const Colors = {
  decibel: "#0B0B0F", card: "#15151C", pink: "#FF4D6A",
  purple: "#9B6DFF", blue: "#4D9AFF", teal: "#00D4AA",
  yellow: "#FFD700", gray: "#8E8E9A", lightGray: "#55556A", white: "#FFFFFF",
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LocationPermissionModal, LocationBanner, and wire into root layout</name>
  <files>
    /home/swarn/decibel-mobile/src/components/location/LocationPermissionModal.tsx
    /home/swarn/decibel-mobile/src/components/location/LocationBanner.tsx
    /home/swarn/decibel-mobile/app/_layout.tsx
  </files>
  <action>
1. Create `src/components/location/LocationPermissionModal.tsx`:
   - Full-screen modal with semi-transparent dark background
   - Shows location pin icon (use MapPin from lucide-react-native)
   - Title: "Know When You're at a Show"
   - Body: the explanationText from useLocation hook
   - Two buttons: "Enable Location" (pink, calls requestPermission then closes) and "Not Now" (text button, closes modal and sets permissionDenied in locationStore)
   - Only shown once (controlled by hasShownExplanation in locationStore)
   - Props: `visible: boolean`, `onClose: () => void`
   - Use Modal from react-native, Animated fade-in
   - Poppins fonts, brand colors, dark card background

2. Create `src/components/location/LocationBanner.tsx`:
   - Non-intrusive banner that slides down from top of screen (below safe area)
   - Uses Reanimated for slide-in animation (translateY from -100 to 0, spring config matching Phase 17 pattern: damping 12, stiffness 180)
   - Renders when `useVenueDetection()` returns non-empty nearbyEvents AND event not dismissed
   - Shows the FIRST non-dismissed event (closest venue)
   - **Single artist layout:** "[Artist photo] [Artist name] is playing at [Venue]. Collect?" with Collect button and X dismiss
   - **Multi-artist layout (LOC-05):** "[Venue name] — [N] artists performing" with expandable list showing each artist with photo, name, and individual "Collect" teal button
   - Each artist's Collect button calls `useCollect().mutate({ performerId })` — on success, show ConfirmationModal (reuse from Phase 17), trigger medium haptic
   - X dismiss button calls `locationStore.dismissEvent(eventId)` — banner slides out, won't return for this event
   - Banner background: Colors.card with 1px bottom border in lightGray, rounded bottom corners
   - Artist photo: 32px circle with gradient fallback (same pattern as Phase 17)
   - Collect button: small teal pill button
   - Position: absolute, top 0, left 0, right 0, zIndex 100 (floats above tab content)
   - When no nearby events (or all dismissed): renders null

3. Modify `app/_layout.tsx`:
   - Import LocationBanner and LocationPermissionModal
   - Add LocationBanner as a sibling AFTER the Stack navigator inside RootNavigator, but only when `session` exists (authenticated)
   - Add LocationPermissionModal: show it once after first login if `!hasShownExplanation && permissionStatus === 'undetermined'`. Use a useEffect with a 2-second delay after auth (don't interrupt the initial app load). When closed, call `setExplanationShown()`.
   - Import useLocation and useLocationStore for the permission check
   - The banner and modal should be OUTSIDE the Stack so they overlay all screens
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - LocationPermissionModal displays explanation and requests foreground-only permission
    - LocationBanner appears as global overlay when near a venue with active event
    - Multi-artist lineups show all artists with individual collect buttons
    - Dismiss persists via locationStore so banner won't reappear for same event
    - Collect from banner creates verified collection via useCollect
    - Root layout renders both components for authenticated users
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Collect tab with "I'm at a show" manual trigger and NearbyVenueSheet</name>
  <files>
    /home/swarn/decibel-mobile/src/components/location/NearbyVenueSheet.tsx
    /home/swarn/decibel-mobile/app/(tabs)/collect.tsx
  </files>
  <action>
1. Create `src/components/location/NearbyVenueSheet.tsx`:
   - Bottom sheet component (use a Modal with slide-up animation, or a View that slides up from bottom using Reanimated)
   - Shows list of nearby venues with active events from useVenueDetection
   - Each venue row: venue name, address, distance (formatted: "150m away"), list of performing artists
   - Each artist has a "Collect" button that calls useCollect, shows ConfirmationModal on success
   - If location denied: show a message "Location access is needed to find nearby venues. You can enable it in Settings." with no crash (LOC-07 graceful degradation)
   - If no nearby venues found: show "No active events detected nearby. Try moving closer to the venue or check back during showtime."
   - Props: `visible: boolean`, `onClose: () => void`

2. Rewrite `app/(tabs)/collect.tsx` (currently a placeholder saying "Coming in Phase 5"):
   - Replace with full Collect tab screen
   - SafeAreaView with dark background
   - Header: "Collect" title in pink, subtitle "Verify your presence at live shows"
   - Main content area:
     a. If location permission granted and nearbyEvents exist: show a card for each nearby event with venue name, artist list, and collect buttons (similar to banner but full-screen layout with more detail)
     b. If location permission granted but no nearby events: show illustration-style empty state with text "No active events nearby" and a subtle "Check again" button that calls refetch()
     c. If location permission not granted: show explanation card with "Enable Location" button that triggers the permission request flow
   - "I'm at a show" button (LOC-06): Large pink outlined button at bottom of screen. Tap behavior:
     - If location granted: calls refetch() on useVenueDetection, then opens NearbyVenueSheet showing results
     - If location denied: shows a message explaining location is needed, with a "Open Settings" button (use Linking.openSettings() from react-native) and a fallback message "Or browse artists on the Search tab to collect manually"
   - Use existing patterns: Poppins fonts, brand colors, SafeAreaView edges={["top"]}
   - The "I'm at a show" button should always be visible regardless of permission state — it's the manual fallback
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Collect tab shows nearby events when at a venue, empty state when not
    - "I'm at a show" button triggers location check and shows nearby venues
    - NearbyVenueSheet lists venues with artists and individual collect buttons
    - Graceful degradation: denied permission shows explanation, not crash
    - All collect actions use existing useCollect hook (capture_method handled server-side)
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes clean
2. `grep -r "LocationBanner" app/_layout.tsx` confirms global overlay wired
3. `grep -r "requestBackgroundPermissions" src/` returns nothing
4. Collect tab no longer shows "Coming in Phase 5"
5. LocationPermissionModal uses explanationText matching CONTEXT.md wording
6. Multi-artist lineup renders individual collect buttons per performer
</verification>

<success_criteria>
- Fan sees explanation modal once before location permission request
- Auto-detection banner appears when at a venue with active event
- Multi-artist lineups show all artists with individual collect buttons
- Dismiss hides banner for that event permanently
- Collect tab has "I'm at a show" manual trigger
- NearbyVenueSheet shows venue selection with artist lineup
- App works fully without location permission (graceful degradation)
- All TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/20-location-based-collection/20-02-SUMMARY.md`
</output>
