---
phase: 13-badges-and-gamification
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/app/passport/passport-client.tsx
  - src/app/passport/badge-showcase.tsx
  - src/app/passport/badge-unlock-toast.tsx
  - src/app/passport/[slug]/page.tsx
autonomous: true
requirements: [BADGE-06, BADGE-07]

must_haves:
  truths:
    - "Fan sees earned badges on their passport with icon, name, description, date earned, and rarity tier"
    - "Public passport also displays earned badges"
    - "When a new badge is unlocked, the fan sees a visual animation or toast notification"
    - "Badge evaluation triggers automatically after discovering or collecting an artist"
  artifacts:
    - path: "src/app/passport/badge-showcase.tsx"
      provides: "Badge grid component showing earned badges with rarity styling"
      min_lines: 40
    - path: "src/app/passport/badge-unlock-toast.tsx"
      provides: "Animated toast/overlay for newly earned badges"
      min_lines: 20
    - path: "src/app/passport/passport-client.tsx"
      provides: "Updated passport with badge section and auto-evaluation after actions"
  key_links:
    - from: "src/app/passport/passport-client.tsx"
      to: "/api/badges"
      via: "fetch on mount to load earned badges"
      pattern: "fetch.*api/badges"
    - from: "src/app/passport/passport-client.tsx"
      to: "/api/badges/evaluate"
      via: "POST after discover/collect actions"
      pattern: "fetch.*api/badges/evaluate"
    - from: "src/app/passport/badge-showcase.tsx"
      to: "src/lib/badges/definitions.ts"
      via: "imports BADGE_DEFINITIONS for display data"
      pattern: "BADGE_DEFINITIONS"
---

<objective>
Display earned badges on the passport with rich visual treatment (rarity-colored borders, icons, descriptions) and trigger unlock animations when new badges are earned after collecting or discovering artists.

Purpose: This makes the passport feel alive and collectible -- fans see their achievements and get rewarded with visual feedback.
Output: Badge showcase component, unlock toast animation, integrated into passport page and public passport.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-badges-and-gamification/13-01-SUMMARY.md

<interfaces>
<!-- Types created in Plan 01 that this plan consumes -->

From src/lib/types/badges.ts (created in Plan 01):
```typescript
type BadgeCategory = "discovery" | "attendance" | "exploration" | "streak" | "social";
type BadgeRarity = "common" | "rare" | "epic" | "legendary";
type BadgeId = string; // literal union of all badge IDs
interface BadgeDefinition { id: BadgeId; name: string; description: string; category: BadgeCategory; rarity: BadgeRarity; icon: string; criteria: string; }
interface EarnedBadge { badge_id: BadgeId; fan_id: string; earned_at: string; }
interface BadgeWithDefinition = EarnedBadge & { definition: BadgeDefinition }
```

From src/lib/badges/definitions.ts (created in Plan 01):
```typescript
export const BADGE_DEFINITIONS: Record<BadgeId, BadgeDefinition>;
export function getBadgesByCategory(category: BadgeCategory): BadgeDefinition[];
```

API endpoints (created in Plan 01):
- GET /api/badges -> { badges: BadgeWithDefinition[] }
- POST /api/badges/evaluate -> { newBadges: BadgeId[], total: number }

From src/app/passport/passport-client.tsx:
```typescript
interface PassportClientProps { fan: PassportFan; fanSlug: string; timeline: PassportTimelineEntry[]; isPublic?: boolean; }
```
- Uses sonner toast library (already imported)
- Has DiscoverModal, SpotifyImport, Recommendations components
- Stats section with gradient numbers
- Timeline grouped by month
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badge showcase component and unlock toast</name>
  <files>src/app/passport/badge-showcase.tsx, src/app/passport/badge-unlock-toast.tsx</files>
  <action>
1. Create `src/app/passport/badge-showcase.tsx`:
   - Props: `{ badges: BadgeWithDefinition[]; isPublic?: boolean }`
   - Renders a "Badges" section header with total count (e.g., "Badges (7)")
   - Grid layout: 2 cols on mobile, 3 on md, 4 on lg
   - Each badge card shows:
     - Emoji icon (large, centered)
     - Badge name (bold)
     - Description (small text, muted)
     - Date earned (small, bottom)
     - Rarity indicator: border color based on rarity
       - common: border-white/20
       - rare: border-purple (use --purple: #9B6DFF)
       - epic: border-pink (use --pink: #FF4D6A)
       - legendary: border-yellow (use --yellow: #FFD700) + subtle glow
   - Category filter tabs at top: All, Discovery, Attendance, Exploration, Streak, Social
   - Empty state: "No badges yet. Start collecting artists to earn your first badge!" (only on non-public view)
   - Dark theme: bg-white/5 card backgrounds, consistent with existing passport aesthetic
   - If isPublic and no badges, hide the entire section

2. Create `src/app/passport/badge-unlock-toast.tsx`:
   - Props: `{ badges: BadgeDefinition[]; onDismiss: () => void }`
   - Full-screen overlay (semi-transparent dark bg) with centered card
   - Shows each newly earned badge with:
     - Large emoji icon with scale-in CSS animation (transform scale 0 -> 1 with spring ease)
     - Badge name in gradient text (pink to purple)
     - Description
     - Rarity label with colored pill
   - If multiple badges earned, show them stacked vertically in the card
   - "Nice!" dismiss button at bottom (pink accent)
   - Auto-dismiss after 5 seconds via setTimeout
   - CSS keyframe animations defined inline via style jsx or tailwind animate classes
   - Use Tailwind's animate-bounce or a custom @keyframes for the icon entrance
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && npx tsc --noEmit src/app/passport/badge-showcase.tsx src/app/passport/badge-unlock-toast.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Badge showcase renders a filterable grid of badges with rarity-styled cards. Unlock toast shows an animated overlay for newly earned badges. Both components compile clean.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate badges into passport and trigger evaluation</name>
  <files>src/app/passport/passport-client.tsx, src/app/passport/[slug]/page.tsx</files>
  <action>
1. Update `src/app/passport/passport-client.tsx`:
   - Import BadgeShowcase, BadgeUnlockToast, and badge types
   - Add state: `badges: BadgeWithDefinition[]`, `newlyEarnedBadges: BadgeDefinition[]`, `showUnlockToast: boolean`
   - On mount (useEffect), fetch GET /api/badges and set badges state (only when not isPublic)
   - Add BadgeShowcase section BETWEEN the stats section and the timeline section
   - Pass `badges` and `isPublic` to BadgeShowcase
   - After a successful discover (in the existing discover modal callback) or Spotify import:
     - POST /api/badges/evaluate
     - If newBadges.length > 0: look up definitions, set newlyEarnedBadges, show unlock toast
     - Also re-fetch GET /api/badges to update the showcase
   - Render BadgeUnlockToast when showUnlockToast is true
   - For the existing sonner toast pattern: ALSO fire a sonner toast "Badge unlocked: {badge.name}!" as a secondary notification (the overlay is the primary one)

2. Update `src/app/passport/[slug]/page.tsx` (public passport):
   - After fetching the fan's collections, also query fan_badges table for this fan
   - Map badge_id values to BADGE_DEFINITIONS to build BadgeWithDefinition array
   - Pass badges to PassportClient (add optional `badges?: BadgeWithDefinition[]` prop if needed, or pass as separate prop)
   - The BadgeShowcase will render in public view too (isPublic=true hides empty state)

   Note: The public passport is a server component. It should fetch fan_badges via admin client and pass the data down. Import BADGE_DEFINITIONS from definitions.ts to map badge IDs to full definitions server-side.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Badges display on passport between stats and timeline. New badges trigger animated unlock overlay after discover/collect actions. Public passport shows earned badges. Build passes clean.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. Badge showcase renders on passport page with category filter tabs
3. Rarity styling applies correct border colors (common=white, rare=purple, epic=pink, legendary=yellow+glow)
4. After discovering an artist, POST /api/badges/evaluate fires and unlock toast appears if new badges earned
5. Public passport at /passport/[slug] shows badges section (hidden if empty)
6. Unlock toast has CSS animation and auto-dismisses after 5 seconds
</verification>

<success_criteria>
- Badges section visible on passport page between stats and timeline
- Each badge shows icon, name, description, earned date, and rarity-colored border
- Category filter tabs work (All/Discovery/Attendance/Exploration/Streak/Social)
- Unlock animation overlay appears when new badges are earned
- Public passport displays earned badges
- Build passes clean with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-badges-and-gamification/13-02-SUMMARY.md`
</output>
