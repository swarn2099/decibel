---
phase: 03-performer-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/dashboard-client.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-08
  - DASH-09

must_haves:
  truths:
    - "Dashboard stats row shows all 4 tiers: Network, Early Access, Secret, Inner Circle plus Total Fans"
    - "Go Live action shows a toast notification confirming the performer is now live"
    - "Stats grid renders correctly at 5 cards on desktop (sm:grid-cols-5)"
  artifacts:
    - path: "src/app/dashboard/dashboard-client.tsx"
      provides: "Secret stat card + Go Live toast"
      contains: "Secret"
  key_links:
    - from: "src/app/dashboard/dashboard-client.tsx"
      to: "sonner toast"
      via: "import { toast } from sonner"
      pattern: "toast\\("
---

<objective>
Add the missing Secret tier stat card to the dashboard overview and add toast notifications to the Go Live action.

Purpose: The dashboard currently shows only 3 of 4 tier cards (missing Secret). Go Live has no user feedback beyond the LIVE badge appearing. This plan fills those gaps.
Output: Updated dashboard-client.tsx with 5 stat cards and toast on Go Live.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/dashboard/dashboard-client.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app/dashboard/dashboard-client.tsx:
```typescript
// Tiers object passed as prop - already includes secret
tiers: { network: number; early_access: number; secret: number; inner_circle: number };

// Tier styling maps - already include secret
const TIER_COLORS: Record<string, { text: string; bg: string }> = {
  secret: { text: "text-blue", bg: "bg-blue/10" },
  // ...
};

const TIER_LABELS: Record<string, string> = {
  secret: "Secret",
  // ...
};

// Current stats row (line 243-248) - only 4 cards, missing Secret:
// Total Fans, Network, Early Access, Inner Circle
// Grid: grid-cols-2 sm:grid-cols-4
```

Sonner toast (already installed in Phase 2, Toaster in root layout):
```typescript
import { toast } from "sonner";
// Usage: toast.success("Message here")
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Secret tier stat card and update grid</name>
  <files>src/app/dashboard/dashboard-client.tsx</files>
  <action>
In the OverviewTab stats row (around line 243):

1. Add import for a Secret tier icon. Use `Eye` from lucide-react (add to existing import). The Eye icon fits "Secret" tier thematically.

2. Change the stats grid from `grid-cols-2 sm:grid-cols-4` to `grid-cols-2 sm:grid-cols-5` to accommodate 5 cards.

3. Add the Secret tier StatCard between Early Access and Inner Circle:
```
<StatCard icon={<Eye size={18} />} label="Secret" value={tiers.secret} color="text-blue" />
```

The final order should be: Total Fans, Network, Early Access, Secret, Inner Circle.

4. Do NOT change any other part of the file in this task.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && grep -n "Secret" src/app/dashboard/dashboard-client.tsx | head -5 && grep "grid-cols-5" src/app/dashboard/dashboard-client.tsx</automated>
  </verify>
  <done>Stats row shows 5 cards: Total Fans + all 4 tiers. Grid uses sm:grid-cols-5.</done>
</task>

<task type="auto">
  <name>Task 2: Add toast notifications to Go Live action</name>
  <files>src/app/dashboard/dashboard-client.tsx</files>
  <action>
1. Add `import { toast } from "sonner";` at the top of the file (sonner is already installed and Toaster is in root layout from Phase 2).

2. In the GoLiveButton `handleGoLive` function (around line 174), after `setLiveStatus("live")`, add:
```typescript
toast.success("You're live! Fans nearby will be notified.");
```

3. Add an error case — if `!res.ok`, show:
```typescript
toast.error("Failed to go live. Try again.");
```

4. Do NOT change message composer toast yet — that's Plan 02.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && grep -n "toast" src/app/dashboard/dashboard-client.tsx | head -10</automated>
  </verify>
  <done>Go Live button shows success toast when confirmed, error toast on failure.</done>
</task>

</tasks>

<verification>
- `npm run build` passes (run after both tasks)
- Dashboard stats row shows 5 cards in the code
- `toast` imported and used in GoLiveButton
</verification>

<success_criteria>
- Secret tier stat card visible in stats row (5 cards total)
- Go Live action triggers toast notification
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-performer-dashboard/03-01-SUMMARY.md`
</output>
