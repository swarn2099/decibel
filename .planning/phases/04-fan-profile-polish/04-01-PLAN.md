---
phase: 04-fan-profile-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tiers.ts
  - src/app/auth/login/page.tsx
  - src/app/auth/callback/route.ts
  - src/middleware.ts
  - src/app/profile/page.tsx
  - src/app/profile/profile-client.tsx
autonomous: true
requirements:
  - AUTH-07
  - FAN-01
  - FAN-02
  - FAN-03
  - FAN-04
  - DEMO-02

must_haves:
  truths:
    - "Fan can sign in via magic link and land on /profile (not /dashboard)"
    - "Fan can see their collected performers in a card grid with tier badges and scan counts"
    - "Fan can see their scan history with dates below the collection grid"
    - "Fan with no collections sees an empty state message"
    - "Tier colors are consistent via shared constants (pink/purple/blue/teal)"
  artifacts:
    - path: "src/lib/tiers.ts"
      provides: "Shared TIER_COLORS, TIER_LABELS, TIER_THRESHOLDS constants"
      exports: ["TIER_COLORS", "TIER_LABELS", "TIER_THRESHOLDS"]
    - path: "src/app/auth/callback/route.ts"
      provides: "Smart redirect — performer to /dashboard, fan to /profile"
      contains: "performers"
    - path: "src/middleware.ts"
      provides: "Route protection for /profile and /settings"
      contains: "/profile"
    - path: "src/app/profile/page.tsx"
      provides: "Server component with auth check and data fetching"
      min_lines: 30
    - path: "src/app/profile/profile-client.tsx"
      provides: "Client component with collection grid and scan history"
      min_lines: 60
  key_links:
    - from: "src/app/auth/callback/route.ts"
      to: "performers table"
      via: "admin client query for claimed_by"
      pattern: "claimed_by.*user\\.id"
    - from: "src/app/profile/page.tsx"
      to: "fan_tiers + collections tables"
      via: "admin client queries"
      pattern: "createSupabaseAdmin"
    - from: "src/app/profile/profile-client.tsx"
      to: "src/lib/tiers.ts"
      via: "import TIER_COLORS, TIER_LABELS"
      pattern: "from.*lib/tiers"
---

<objective>
Build the fan authentication flow and profile page — the fan-facing "passport" showing collected performers with tier badges, scan counts, and scan history.

Purpose: Fans need a destination after magic link login. The profile page is the core fan value — a visual record of every performer they've collected at live shows.
Output: Working /profile page with collection grid and scan history, smart auth redirect, shared tier constants.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fan-profile-polish/04-CONTEXT.md
@.planning/phases/04-fan-profile-polish/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/supabase.ts:
```typescript
export { createSupabaseBrowser } from "./supabase-browser";
export { createSupabaseServer } from "./supabase-server";
export { createSupabaseAdmin } from "./supabase-admin";
```

From src/app/dashboard/dashboard-client.tsx (TIER constants to extract):
```typescript
const TIER_COLORS: Record<string, { text: string; bg: string }> = {
  network: { text: "text-pink", bg: "bg-pink/10" },
  early_access: { text: "text-purple", bg: "bg-purple/10" },
  secret: { text: "text-blue", bg: "bg-blue/10" },
  inner_circle: { text: "text-teal", bg: "bg-teal/10" },
};
const TIER_LABELS: Record<string, string> = {
  network: "Network",
  early_access: "Early Access",
  secret: "Secret",
  inner_circle: "Inner Circle",
};
```

From src/middleware.ts:
```typescript
export const config = {
  matcher: ["/dashboard/:path*"],
};
```

From src/app/auth/callback/route.ts:
```typescript
// Currently always redirects to /dashboard — needs smart redirect
return NextResponse.redirect(new URL("/dashboard", req.url));
```

From src/app/auth/login/page.tsx:
```typescript
// Currently says "Performer Dashboard" — needs to say "Sign In"
<p className="mb-8 text-center text-sm text-gray">Performer Dashboard</p>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared tier constants and update auth flow</name>
  <files>src/lib/tiers.ts, src/app/auth/login/page.tsx, src/app/auth/callback/route.ts, src/middleware.ts, src/app/dashboard/dashboard-client.tsx</files>
  <action>
1. Create `src/lib/tiers.ts` with exported TIER_COLORS, TIER_LABELS, and TIER_THRESHOLDS constants (use the exact values from dashboard-client.tsx — see interfaces above). Export all three as named exports.

2. Update `src/app/dashboard/dashboard-client.tsx` to import TIER_COLORS and TIER_LABELS from `@/lib/tiers` instead of defining them inline. Remove the inline const definitions.

3. Update `src/app/auth/login/page.tsx`: Change the subtitle from "Performer Dashboard" to "Sign In" so the page works for both fans and performers.

4. Update `src/app/auth/callback/route.ts` to implement smart redirect:
   - After exchangeCodeForSession, call getUser()
   - If user exists, use admin client to check if user has a claimed performer: `admin.from("performers").select("id").eq("claimed_by", user.id).single()`
   - If performer found → redirect to /dashboard
   - Otherwise (fan or unclaimed) → redirect to /profile
   - Wrap the performer lookup in try/catch — default to /profile on any error (avoid redirect loops)

5. Update `src/middleware.ts`: Extend the matcher array to include /profile and /settings routes. Also extend the pathname check to protect those routes:
   ```
   matcher: ["/dashboard/:path*", "/profile/:path*", "/settings/:path*"]
   ```
   Update the `if` condition to check all three path prefixes, redirecting unauthenticated users to /auth/login.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Shared tiers.ts exists with TIER_COLORS/TIER_LABELS/TIER_THRESHOLDS exports. Dashboard imports from shared lib. Login page says "Sign In". Auth callback does smart redirect (performer->dashboard, fan->profile). Middleware protects /profile and /settings.</done>
</task>

<task type="auto">
  <name>Task 2: Build fan profile page with collection grid and scan history</name>
  <files>src/app/profile/page.tsx, src/app/profile/profile-client.tsx</files>
  <action>
1. Create `src/app/profile/page.tsx` as a server component following the exact pattern from dashboard/page.tsx:
   - Import createSupabaseServer and createSupabaseAdmin from @/lib/supabase
   - Auth check: getUser(), redirect to /auth/login if no user
   - Find fan by email: `admin.from("fans").select("id, email, name").eq("email", user.email).single()`
   - If no fan found, render ProfileClient with fan=null and empty collections/scanHistory arrays (empty state)
   - If fan found, query collections with tier info: `admin.from("fan_tiers").select("scan_count, current_tier, last_scan_date, performers!inner(id, name, slug, photo_url, genres, city)").eq("fan_id", fan.id).order("last_scan_date", { ascending: false })`
   - Query scan history: `admin.from("collections").select("id, event_date, capture_method, created_at, performers!inner(name, slug), venues(name)").eq("fan_id", fan.id).order("created_at", { ascending: false }).limit(50)`
   - Pass fan, collections, scanHistory to ProfileClient

2. Create `src/app/profile/profile-client.tsx` as a "use client" component:
   - Import TIER_COLORS, TIER_LABELS from @/lib/tiers
   - Import Link from next/link, icons from lucide-react (User, Clock, Settings, LogOut)
   - Import createSupabaseBrowser from @/lib/supabase, useRouter from next/navigation

   **Header section:**
   - Show fan name or email, link to /settings (Settings icon), logout button (LogOut icon)
   - Logout: createSupabaseBrowser().auth.signOut() then router.push("/")
   - Dark bg-bg background, Poppins styling

   **Collection grid (main section):**
   - If no collections: show empty state card — "You haven't collected any performers yet. Scan a QR code at your next show to get started." with a subtle gradient border
   - If collections exist: responsive grid (grid-cols-2 on mobile, grid-cols-3 on md+)
   - Each card: performer photo (rounded, fallback to initials if no photo_url), performer name, city, tier badge (colored pill using TIER_COLORS[tier].text and TIER_COLORS[tier].bg), scan count
   - Cards link to /artist/[slug] using Next.js Link
   - Cards use bg-bg-card, border-light-gray/10, hover:border-pink/30 transition

   **Scan history section (below grid):**
   - Heading: "Scan History" with Clock icon
   - If no scans: don't render section
   - Chronological list: each row shows performer name (link to /artist/[slug]), venue name (if available), date (formatted as relative or short date), capture method badge
   - Use bg-bg-card cards or subtle list items with border-b-light-gray/10 dividers
   - Design: dark aesthetic matching the rest of the app
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Fan profile page renders at /profile. Authenticated fan sees collection grid with performer photos, tier badges, scan counts, and clickable cards linking to /artist/[slug]. Scan history shows below with dates and venues. Empty state shows for fans with no collections. npm run build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- src/lib/tiers.ts exports TIER_COLORS, TIER_LABELS, TIER_THRESHOLDS
- Auth callback contains performer lookup logic with /profile default
- Middleware matcher includes /dashboard, /profile, /settings
- Login page subtitle reads "Sign In" not "Performer Dashboard"
- Profile page server component fetches data with admin client
- Profile client component imports from @/lib/tiers
</verification>

<success_criteria>
1. Shared tier constants extracted and used by both dashboard and profile
2. Fan magic link login redirects to /profile (not /dashboard)
3. Profile page shows collection grid with tier badges and scan counts
4. Profile page shows scan history with dates
5. Empty state renders for fans with no collections
6. All protected routes (/dashboard, /profile, /settings) redirect unauthenticated users to login
7. npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-fan-profile-polish/04-01-SUMMARY.md`
</output>
