---
phase: 07-content-generator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/content/renderer.ts
  - scripts/content/spotlight.ts
  - scripts/content/roundup.ts
  - scripts/content/teaser.ts
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-06]

must_haves:
  truths:
    - "Running spotlight script with a performer slug produces a 1080x1080 PNG and companion caption .txt"
    - "Running roundup script produces a weekly recap PNG and caption .txt from real event data"
    - "Running teaser script with a feature name produces a phone mockup PNG and caption .txt"
    - "All images render via Playwright screenshot at 1080x1080"
  artifacts:
    - path: "scripts/content/renderer.ts"
      provides: "Playwright-based HTML-to-PNG renderer + caption saver"
      exports: ["renderToImage", "saveCaption", "wrapInTemplate"]
    - path: "scripts/content/spotlight.ts"
      provides: "DJ Spotlight card generator with performer photo, stats, genres"
      exports: ["generateSpotlight"]
    - path: "scripts/content/roundup.ts"
      provides: "Scene Roundup weekly recap card from event data"
      exports: ["generateRoundup"]
    - path: "scripts/content/teaser.ts"
      provides: "Product Teaser phone mockup card"
      exports: ["generateTeaser"]
  key_links:
    - from: "scripts/content/spotlight.ts"
      to: "scripts/content/renderer.ts"
      via: "renderToImage + wrapInTemplate"
      pattern: "renderToImage.*outputPath"
    - from: "scripts/content/spotlight.ts"
      to: "supabase"
      via: "getSupabase() queries for performer + events + fan_tiers"
      pattern: "supabase\\.from"
---

<objective>
Test and fix all three content generators (spotlight, roundup, teaser) to produce valid 1080x1080 PNG images with companion caption text files.

Purpose: The content scripts were scaffolded in a prior session but never verified end-to-end. This plan runs each generator against real DB data, fixes any bugs, and confirms output matches requirements.
Output: Three working content generators that each produce PNG + TXT output.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/content/renderer.ts
@scripts/content/spotlight.ts
@scripts/content/roundup.ts
@scripts/content/teaser.ts
@scripts/scrapers/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test and fix individual content generators</name>
  <files>scripts/content/renderer.ts, scripts/content/spotlight.ts, scripts/content/roundup.ts, scripts/content/teaser.ts</files>
  <action>
Run each content generator against real DB data and fix any bugs:

1. **Spotlight**: Run `npx tsx scripts/content/spotlight.ts <slug>` with a real performer slug from the DB (query performers table for one with photo_url and genres). Verify it produces a PNG at `content/output/YYYY-MM-DD-spotlight-<slug>.png` and a matching `.txt` caption file. Fix any Supabase query issues, HTML rendering bugs, or Playwright errors.

2. **Roundup**: Run `npx tsx scripts/content/roundup.ts 7` to generate a weekly recap. Verify it queries real events from the last 7 days, produces a PNG and caption .txt. If no events in last 7 days, increase to 30 or 90 days for testing. Fix any date filtering or join issues.

3. **Teaser**: Run `npx tsx scripts/content/teaser.ts passport` to generate a product teaser. Verify PNG and caption .txt output. This one doesn't hit the DB so should be the simplest.

4. **Verify output**: For each generated PNG, confirm the file exists, is non-zero size, and check dimensions are 1080x1080 using `identify` or `file` command. Confirm companion .txt file exists and contains caption text.

If any script fails:
- Fix the bug in the relevant file
- Re-run to confirm the fix works
- Common issues to watch for: Playwright browser launch failures (may need `--no-sandbox`), Google Fonts loading timeout (renderer uses @import for Poppins), Supabase query type casting issues

If Playwright needs `--no-sandbox`, update `renderer.ts` to add `args: ['--no-sandbox']` to chromium.launch().
  </action>
  <verify>
    <automated>npx tsx scripts/content/spotlight.ts $(npx tsx -e "const{createClient}=require('@supabase/supabase-js');require('dotenv').config({path:'.env.local'});const s=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY);s.from('performers').select('slug').not('photo_url','is',null).limit(1).single().then(r=>console.log(r.data.slug))" 2>/dev/null) 2>&1 | grep -q "Generated" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All three generators (spotlight, roundup, teaser) successfully produce PNG + TXT output files in content/output/. PNG files are 1080x1080. No script crashes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify output quality and dimensions</name>
  <files>content/output/.gitkeep</files>
  <action>
After Task 1 generates files, validate the full output set:

1. List all files in `content/output/` and confirm PNG + TXT pairs exist for each type (spotlight, roundup, teaser).

2. For each PNG, verify:
   - File size > 10KB (not a blank/corrupt image)
   - Dimensions are 1080x1080 (use `file` or `python3 -c "from PIL import Image; img=Image.open('file.png'); print(img.size)"`)

3. For each TXT, verify:
   - File is non-empty
   - Contains hashtags (all captions should have #decibel)
   - Spotlight caption mentions the performer name
   - Roundup caption mentions artist/venue count

4. Clean up test output: Remove generated test files from `content/output/` (keep .gitkeep).

Report: Print a summary table showing each generated file, its size, dimensions, and pass/fail status.
  </action>
  <verify>
    <automated>ls content/output/*.png 2>/dev/null | head -1 | xargs -I{} file {} | grep -q "PNG" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All three content types produce valid 1080x1080 PNG images with non-empty companion caption files containing relevant text and hashtags.</done>
</task>

</tasks>

<verification>
- `npx tsx scripts/content/spotlight.ts <any-slug>` produces PNG + TXT without errors
- `npx tsx scripts/content/roundup.ts` produces PNG + TXT without errors
- `npx tsx scripts/content/teaser.ts passport` produces PNG + TXT without errors
- All PNGs are 1080x1080
- All TXT files contain captions with #decibel hashtag
</verification>

<success_criteria>
Each of the three content generators (spotlight, roundup, teaser) runs end-to-end against real data and produces valid PNG + TXT output files.
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-generator/07-01-SUMMARY.md`
</output>
