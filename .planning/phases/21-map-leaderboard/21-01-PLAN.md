---
phase: 21-map-leaderboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/swarn/decibel-mobile/app/(tabs)/map.tsx
  - /home/swarn/decibel-mobile/src/hooks/useMapVenues.ts
  - /home/swarn/decibel-mobile/src/components/map/VenueMarker.tsx
  - /home/swarn/decibel-mobile/src/components/map/VenueBottomSheet.tsx
  - /home/swarn/decibel-mobile/src/components/map/GenreFilterChips.tsx
  - /home/swarn/decibel-mobile/src/components/map/mapStyle.ts
  - /home/swarn/decibel-mobile/src/types/index.ts
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
  - MAP-05
  - MAP-06

must_haves:
  truths:
    - "Fan can view a full-screen dark-themed map with venue markers on the Map tab"
    - "Venue markers are sized by activity level and colored by genre"
    - "Fan can tap a venue marker to see a bottom sheet with venue name, events, and artists"
    - "Fan can filter the map by genre using chips at the top"
    - "Fan can toggle Tonight mode to show only active venues with pulsing markers"
    - "Fan can tap Near Me to center map on their location"
  artifacts:
    - path: "/home/swarn/decibel-mobile/app/(tabs)/map.tsx"
      provides: "Full-screen map screen replacing placeholder"
      min_lines: 80
    - path: "/home/swarn/decibel-mobile/src/hooks/useMapVenues.ts"
      provides: "Data hook fetching venues with events from Supabase"
      exports: ["useMapVenues"]
    - path: "/home/swarn/decibel-mobile/src/components/map/VenueMarker.tsx"
      provides: "Custom map marker component with genre color and activity sizing"
      exports: ["VenueMarker"]
    - path: "/home/swarn/decibel-mobile/src/components/map/VenueBottomSheet.tsx"
      provides: "Bottom sheet showing venue details, lineup, navigate button"
      exports: ["VenueBottomSheet"]
    - path: "/home/swarn/decibel-mobile/src/components/map/GenreFilterChips.tsx"
      provides: "Horizontal genre filter chips"
      exports: ["GenreFilterChips"]
    - path: "/home/swarn/decibel-mobile/src/components/map/mapStyle.ts"
      provides: "Custom dark JSON style for react-native-maps"
      exports: ["darkMapStyle"]
  key_links:
    - from: "/home/swarn/decibel-mobile/app/(tabs)/map.tsx"
      to: "useMapVenues hook"
      via: "TanStack Query hook returning venue data"
      pattern: "useMapVenues"
    - from: "/home/swarn/decibel-mobile/src/hooks/useMapVenues.ts"
      to: "supabase"
      via: "Direct Supabase query for venues with events and performers"
      pattern: "supabase\\.from.*venues"
    - from: "VenueMarker tap"
      to: "VenueBottomSheet"
      via: "selectedVenue state triggers bottom sheet presentation"
      pattern: "selectedVenue"
    - from: "VenueBottomSheet artist tap"
      to: "/artist/[slug]"
      via: "router.push to artist profile"
      pattern: "router\\.push.*artist"
---

<objective>
Build the full-screen dark-themed scene map for the Map tab with venue markers, genre filtering, "Tonight" mode, "Near Me" centering, and venue detail bottom sheet.

Purpose: The map is the visual centerpiece of the "scene" — fans explore their city's underground venues and see what's happening tonight. This replaces the placeholder Map tab.
Output: Working Map tab with interactive venue markers, filtering, and venue detail sheets.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-map-leaderboard/21-CONTEXT.md

Source files to reference:
@/home/swarn/decibel-mobile/app/(tabs)/map.tsx (current placeholder to replace)
@/home/swarn/decibel-mobile/app/(tabs)/_layout.tsx (tab layout — map is already a tab)
@/home/swarn/decibel-mobile/src/hooks/useLocation.ts (existing location hook with getCurrentPosition)
@/home/swarn/decibel-mobile/src/hooks/useVenueDetection.ts (existing venue query patterns)
@/home/swarn/decibel-mobile/src/types/index.ts (existing Venue, Performer types)
@/home/swarn/decibel-mobile/src/constants/colors.ts (brand colors)
@/home/swarn/decibel-mobile/src/lib/supabase.ts (Supabase client)

Web API reference (query pattern to mirror):
@/home/swarn/decibel/src/app/api/map/route.ts (web map API — same Supabase query shape)
@/home/swarn/decibel/src/lib/types/map.ts (MapVenue, MapEvent types)

<interfaces>
<!-- Existing types from src/types/index.ts -->
export type Venue = {
  id: string; name: string; slug: string;
  address: string | null; lat: number | null; lng: number | null;
  geofence_radius: number | null;
};

export type Performer = {
  id: string; name: string; slug: string; photo_url: string | null;
  genres: string[] | null; city: string | null; ...
};

<!-- From web map types (to port) -->
export interface MapEvent {
  id: string; event_date: string; start_time: string | null;
  performer_name: string; performer_slug: string; performer_photo: string | null;
}

export interface MapVenue {
  id: string; name: string; slug: string;
  latitude: number; longitude: number; event_count: number;
  genres: string[]; upcoming_events: MapEvent[];
}

<!-- From useLocation hook -->
export function useLocation(): {
  permissionStatus: PermissionStatus;
  hasPermission: boolean;
  getCurrentPosition: () => Promise<{ latitude: number; longitude: number } | null>;
  requestPermission: () => Promise<PermissionStatus>;
};

<!-- Colors -->
Colors = { decibel: "#0B0B0F", card: "#15151C", pink: "#FF4D6A", purple: "#9B6DFF", blue: "#4D9AFF", teal: "#00D4AA", yellow: "#FFD700", gray: "#8E8E9A" };
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-native-maps, add types and data hook</name>
  <files>
    /home/swarn/decibel-mobile/src/types/index.ts,
    /home/swarn/decibel-mobile/src/hooks/useMapVenues.ts,
    /home/swarn/decibel-mobile/src/components/map/mapStyle.ts
  </files>
  <action>
    1. Install react-native-maps: `npx expo install react-native-maps`. Also install @gorhom/bottom-sheet if not already present: `npx expo install @gorhom/bottom-sheet` (it depends on react-native-reanimated and react-native-gesture-handler which are already installed).

    2. Add MapVenue and MapEvent types to src/types/index.ts (port from web's src/lib/types/map.ts):
    ```
    export type MapEvent = {
      id: string; event_date: string; start_time: string | null;
      performer_name: string; performer_slug: string; performer_photo: string | null;
    };
    export type MapVenue = {
      id: string; name: string; slug: string;
      latitude: number; longitude: number; event_count: number;
      genres: string[]; upcoming_events: MapEvent[];
    };
    ```

    3. Create src/hooks/useMapVenues.ts — a TanStack Query hook that:
    - Accepts `{ genre?: string; tonight?: boolean }` filter params
    - Queries Supabase directly (NOT the web API — mobile uses direct queries per prior decisions)
    - Query: `supabase.from("venues").select("id, name, slug, latitude, longitude, events!inner(id, event_date, start_time, external_url, performer:performers(name, slug, photo_url, genres))")` with `.gte("events.event_date", todayDate)`
    - If tonight=true, filter `.eq("events.event_date", todayDate)`
    - Transform results into MapVenue[] (same logic as web API route.ts — collect genres, build event list, filter by genre client-side)
    - staleTime: 5 minutes
    - Return `{ venues, isLoading, refetch, error }`

    4. Create src/components/map/mapStyle.ts — export `darkMapStyle` as a JSON array for Google Maps / Apple Maps custom styling:
    - Nearly black water (#0B0B0F)
    - Very dark land (#12121A)
    - Muted road labels (#2A2A3A)
    - No POI labels, no transit labels
    - Minimal road geometry in dark gray
    - The style should make venue markers the visual focus
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>react-native-maps installed, MapVenue/MapEvent types exported, useMapVenues hook compiles, dark map style JSON ready</done>
</task>

<task type="auto">
  <name>Task 2: Build map screen with markers, genre filter, tonight toggle, near me</name>
  <files>
    /home/swarn/decibel-mobile/app/(tabs)/map.tsx,
    /home/swarn/decibel-mobile/src/components/map/VenueMarker.tsx,
    /home/swarn/decibel-mobile/src/components/map/GenreFilterChips.tsx
  </files>
  <action>
    1. Create src/components/map/VenueMarker.tsx:
    - Custom marker component receiving a MapVenue
    - Color determined by primary genre: house=#FF4D6A (pink), techno=#4D9AFF (blue), bass=#00D4AA (teal), default=#9B6DFF (purple). Derive primary genre as first genre in venue.genres array.
    - Size determined by event_count: base 12px, +2px per event, max 24px
    - When `tonight` mode is active and venue has no events today: render at 10% opacity
    - When `tonight` mode is active and venue HAS events today: wrap in Animated.View with a breathing/pulsing animation using react-native-reanimated (useAnimatedStyle with withRepeat + withTiming for scale 1.0 -> 1.3 -> 1.0, duration 2000ms)
    - Marker is a filled circle (View with borderRadius) — NOT a pin icon
    - onPress callback for marker selection

    2. Create src/components/map/GenreFilterChips.tsx:
    - Horizontal ScrollView of genre chip buttons: "All", "House", "Techno", "Bass", "Disco", "Drum & Bass"
    - Selected chip gets brand color fill, unselected gets card (#15151C) background
    - Props: `selectedGenre: string | null`, `onSelectGenre: (genre: string | null) => void`
    - Position absolutely at top of map with safe area inset padding

    3. Replace app/(tabs)/map.tsx placeholder with full map screen:
    - Full-screen MapView from react-native-maps with customMapStyle={darkMapStyle}
    - Provider: leave as default (Apple Maps on iOS, Google Maps on Android)
    - Initial region: Chicago (lat: 41.8827, lng: -87.6233, latDelta: 0.08, lngDelta: 0.08)
    - State: `genre` filter (string | null), `tonight` toggle (boolean), `selectedVenue` (MapVenue | null)
    - Render VenueMarker for each venue from useMapVenues({ genre, tonight })
    - GenreFilterChips overlay at top
    - "Tonight" toggle button: positioned top-right below filter chips. When active, icon pulses pink. Use a simple TouchableOpacity with text "Tonight" and a circle indicator.
    - "Near Me" button: positioned bottom-right (above tab bar). Uses existing useLocation().getCurrentPosition to get coords, then animates map to that region. Show as a circular button with a crosshair/location icon from lucide-react-native (Crosshair or LocateFixed).
    - If location permission not granted and user taps Near Me, call requestPermission first.
    - Edge-to-edge: no SafeAreaView wrapping the map itself. Overlays (chips, buttons) use SafeAreaView insets for positioning.
    - Tab bar overlays bottom of map (already handled by tab layout).
    - When a marker is tapped, set selectedVenue state (bottom sheet handled in next task).
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Map tab shows dark-themed full-screen map with colored/sized venue markers, genre filter chips, tonight toggle with pulsing markers, and near-me button</done>
</task>

<task type="auto">
  <name>Task 3: Venue detail bottom sheet with lineup and navigation</name>
  <files>
    /home/swarn/decibel-mobile/src/components/map/VenueBottomSheet.tsx,
    /home/swarn/decibel-mobile/app/(tabs)/map.tsx
  </files>
  <action>
    1. Create src/components/map/VenueBottomSheet.tsx using @gorhom/bottom-sheet:
    - Props: `venue: MapVenue | null`, `onClose: () => void`
    - Dark background (#15151C), no white anywhere
    - Content layout:
      a. Venue name in bold Poppins, white text
      b. Address line in gray (if available — may need to add address to the venue query in useMapVenues; add it)
      c. "Tonight's Lineup" or "Upcoming Events" header
      d. List of events: each row shows performer photo (32px circle, fallback to gradient initials), performer name, event date formatted as "Sat, Mar 9", start_time if available
      e. Each artist row is tappable — onPress navigates to `/artist/${performer_slug}` via router.push
      f. "Navigate" button at bottom — opens Apple Maps / Google Maps with venue coordinates using `Linking.openURL("https://maps.apple.com/?ll=${lat},${lng}")` on iOS or `geo:${lat},${lng}` intent on Android. Use Platform.select.
    - Bottom sheet snap points: ["35%", "60%"]
    - Handle indicator color: gray (#8E8E9A)
    - Background style: backgroundColor #15151C, borderTopLeftRadius 16, borderTopRightRadius 16
    - enablePanDownToClose: true, onClose callback

    2. Wire into map.tsx:
    - Import VenueBottomSheet
    - Wrap the screen content in GestureHandlerRootView (required by @gorhom/bottom-sheet) if not already wrapped at app root
    - When selectedVenue is set (marker tapped), render VenueBottomSheet with that venue
    - When bottom sheet closes, set selectedVenue to null
    - Check if GestureHandlerRootView is already in app/_layout.tsx. If yes, don't add another. If no, wrap map screen content in it.

    3. Ensure the venue query in useMapVenues also selects `address` from venues (add to select string).
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Tapping a venue marker opens a dark bottom sheet showing venue name, event lineup with artist photos, tappable artist rows navigating to profiles, and a Navigate button opening native maps</done>
</task>

</tasks>

<verification>
1. `cd /home/swarn/decibel-mobile && npx tsc --noEmit` — TypeScript compiles cleanly
2. `cd /home/swarn/decibel-mobile && npx expo export --platform ios 2>&1 | tail -5` — bundle builds without errors
3. Map tab no longer shows placeholder text
4. VenueMarker, VenueBottomSheet, GenreFilterChips all export correctly
</verification>

<success_criteria>
- Map tab renders full-screen dark map with custom styling
- Venue markers appear colored by genre and sized by activity
- Genre filter chips filter markers on the map
- "Tonight" toggle fades inactive venues and pulses active ones
- "Near Me" centers map on user location
- Tapping a marker opens a dark bottom sheet with venue details and lineup
- Tapping an artist in the bottom sheet navigates to their profile
- "Navigate" button opens native maps app
</success_criteria>

<output>
After completion, create `.planning/phases/21-map-leaderboard/21-01-SUMMARY.md`
</output>
